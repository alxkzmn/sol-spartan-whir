// SPDX-License-Identifier: MIT
pragma solidity ^0.8.28;
// Generated by script/babybear/generate_eq_variants.py.
import { KoalaBearExt5 } from "../../src/field/KoalaBearExt5.sol";
import { BabyBearBenchField } from "./BabyBearSelectVariants.sol";
import { BabyBearPackedField, KoalaBearPackedField } from "./BabyBearPackedFields.sol";

library KoalaBearEqBaseline {
    function _eqTerm(uint256 p, uint256 q) internal pure returns (uint256) {
        uint256 out;
        assembly ("memory-safe") {
            let M := 0x7f000001
            let mask := 0xffffffff

            let p0 := shr(224, p)
            let p1 := and(shr(192, p), mask)
            let p2 := and(shr(160, p), mask)
            let p3 := and(shr(128, p), mask)
            let p4 := and(shr(96, p), mask)

            let q0 := shr(224, q)
            let q1 := and(shr(192, q), mask)
            let q2 := and(shr(160, q), mask)
            let q3 := and(shr(128, q), mask)
            let q4 := and(shr(96, q), mask)

            let c0 := mul(p0, q0)
            let c1 := add(mul(p0, q1), mul(p1, q0))
            let c2 := add(add(mul(p0, q2), mul(p1, q1)), mul(p2, q0))
            let c3 := add(add(add(mul(p0, q3), mul(p1, q2)), mul(p2, q1)), mul(p3, q0))
            let c4 :=
                add(add(add(add(mul(p0, q4), mul(p1, q3)), mul(p2, q2)), mul(p3, q1)), mul(p4, q0))
            let c5 := add(add(add(mul(p1, q4), mul(p2, q3)), mul(p3, q2)), mul(p4, q1))
            let c6 := add(add(mul(p2, q4), mul(p3, q3)), mul(p4, q2))
            let c7 := add(mul(p3, q4), mul(p4, q3))
            let c8 := mul(p4, q4)
            let bias := shl(35, M)

            let m0 := mod(add(add(c0, c5), sub(bias, c8)), M)
            let m1 := mod(add(c1, c6), M)
            let m2 := mod(add(add(add(c2, sub(bias, c5)), c7), c8), M)
            let m3 := mod(add(add(c3, sub(bias, c6)), c8), M)
            let m4 := mod(add(c4, sub(bias, c7)), M)

            let b := shl(2, M)
            let o0 := mod(add(add(add(1, shl(1, m0)), b), sub(0, add(p0, q0))), M)
            let o1 := mod(add(add(shl(1, m1), b), sub(0, add(p1, q1))), M)
            let o2 := mod(add(add(shl(1, m2), b), sub(0, add(p2, q2))), M)
            let o3 := mod(add(add(shl(1, m3), b), sub(0, add(p3, q3))), M)
            let o4 := mod(add(add(shl(1, m4), b), sub(0, add(p4, q4))), M)

            out := or(
                or(or(shl(224, o0), shl(192, o1)), or(shl(160, o2), shl(128, o3))),
                shl(96, o4)
            )
        }
        return out;
    }

    function _evaluateFixedEqTermsBlobRaw(
        bytes calldata blob,
        uint256 statementPointOffset,
        uint256 initialOodPoint,
        uint256 round0OodPoint,
        uint256 round1OodPoint,
        uint256 round2OodPoint,
        uint256[] memory fullPoint
    )
        internal
        pure
        returns (
            uint256 statementEq,
            uint256 initialEq,
            uint256 round0Eq,
            uint256 round1Eq,
            uint256 round2Eq
        )
    {
        statementEq = KoalaBearExt5.ONE;
        initialEq = KoalaBearExt5.ONE;
        round0Eq = KoalaBearExt5.ONE;
        round1Eq = KoalaBearExt5.ONE;
        round2Eq = KoalaBearExt5.ONE;

        uint256 initialCurrent = initialOodPoint;
        uint256 round0Current = round0OodPoint;
        uint256 round1Current = round1OodPoint;
        uint256 round2Current = round2OodPoint;
        uint256 pointBase;
        assembly ("memory-safe") {
            pointBase := add(fullPoint, 0x20)
        }

        unchecked {
            for (uint256 i = 22; i > 0; --i) {
                uint256 q;
                uint256 statementPointValue;
                assembly ("memory-safe") {
                    let idx := sub(i, 1)
                    q := mload(add(pointBase, shl(5, idx)))
                    statementPointValue := and(
                        calldataload(add(add(blob.offset, statementPointOffset), mul(20, idx))),
                        not(sub(shl(96, 1), 1))
                    )
                }

                statementEq = KoalaBearExt5.mul(statementEq, _eqTerm(statementPointValue, q));
                initialEq = KoalaBearExt5.mul(initialEq, _eqTerm(initialCurrent, q));
                initialCurrent = KoalaBearExt5.square(initialCurrent);

                if (i > 4) {
                    round0Eq = KoalaBearExt5.mul(round0Eq, _eqTerm(round0Current, q));
                    round0Current = KoalaBearExt5.square(round0Current);
                    if (i > 8) {
                        round1Eq = KoalaBearExt5.mul(round1Eq, _eqTerm(round1Current, q));
                        round1Current = KoalaBearExt5.square(round1Current);
                        if (i > 12) {
                            round2Eq = KoalaBearExt5.mul(round2Eq, _eqTerm(round2Current, q));
                            round2Current = KoalaBearExt5.square(round2Current);
                        }
                    }
                }
            }
        }
    }
}

contract KoalaBearEqBaselineHarness {
    function mul(uint256 a, uint256 b) external pure returns (uint256) {
        return KoalaBearExt5.mul(a, b);
    }

    function square(uint256 a) external pure returns (uint256) {
        return KoalaBearExt5.square(a);
    }

    function eq(uint256 a, uint256 b) external pure returns (uint256) {
        return KoalaBearEqBaseline._eqTerm(a, b);
    }

    function squares(uint256 a, uint256 n) external view returns (uint256 used, uint256 result) {
        uint256 start = gasleft();
        result = a;
        for (uint256 i; i < n; ++i) {
            result = KoalaBearExt5.square(result);
        }
        used = start - gasleft();
    }

    function prepare(bytes calldata statement, uint256[4] memory ood, uint256[] memory point)
        external
        view
        returns (uint256 used, uint256[5] memory result)
    {
        require(statement.length == 440 && point.length == 22, "SHAPE");
        uint256 start = gasleft();
        (result[0], result[1], result[2], result[3], result[4]) =
            KoalaBearEqBaseline._evaluateFixedEqTermsBlobRaw(
                statement, 0, ood[0], ood[1], ood[2], ood[3], point
            );
        used = start - gasleft();
    }
}

library KoalaBearEqPeel {
    function _eqTerm(uint256 p, uint256 q) internal pure returns (uint256) {
        uint256 out;
        assembly ("memory-safe") {
            let M := 0x7f000001
            let mask := 0xffffffff

            let p0 := shr(224, p)
            let p1 := and(shr(192, p), mask)
            let p2 := and(shr(160, p), mask)
            let p3 := and(shr(128, p), mask)
            let p4 := and(shr(96, p), mask)

            let q0 := shr(224, q)
            let q1 := and(shr(192, q), mask)
            let q2 := and(shr(160, q), mask)
            let q3 := and(shr(128, q), mask)
            let q4 := and(shr(96, q), mask)

            let c0 := mul(p0, q0)
            let c1 := add(mul(p0, q1), mul(p1, q0))
            let c2 := add(add(mul(p0, q2), mul(p1, q1)), mul(p2, q0))
            let c3 := add(add(add(mul(p0, q3), mul(p1, q2)), mul(p2, q1)), mul(p3, q0))
            let c4 :=
                add(add(add(add(mul(p0, q4), mul(p1, q3)), mul(p2, q2)), mul(p3, q1)), mul(p4, q0))
            let c5 := add(add(add(mul(p1, q4), mul(p2, q3)), mul(p3, q2)), mul(p4, q1))
            let c6 := add(add(mul(p2, q4), mul(p3, q3)), mul(p4, q2))
            let c7 := add(mul(p3, q4), mul(p4, q3))
            let c8 := mul(p4, q4)
            let bias := shl(35, M)

            let m0 := mod(add(add(c0, c5), sub(bias, c8)), M)
            let m1 := mod(add(c1, c6), M)
            let m2 := mod(add(add(add(c2, sub(bias, c5)), c7), c8), M)
            let m3 := mod(add(add(c3, sub(bias, c6)), c8), M)
            let m4 := mod(add(c4, sub(bias, c7)), M)

            let b := shl(2, M)
            let o0 := mod(add(add(add(1, shl(1, m0)), b), sub(0, add(p0, q0))), M)
            let o1 := mod(add(add(shl(1, m1), b), sub(0, add(p1, q1))), M)
            let o2 := mod(add(add(shl(1, m2), b), sub(0, add(p2, q2))), M)
            let o3 := mod(add(add(shl(1, m3), b), sub(0, add(p3, q3))), M)
            let o4 := mod(add(add(shl(1, m4), b), sub(0, add(p4, q4))), M)

            out := or(
                or(or(shl(224, o0), shl(192, o1)), or(shl(160, o2), shl(128, o3))),
                shl(96, o4)
            )
        }
        return out;
    }

    function _evaluateFixedEqTermsBlobRaw(
        bytes calldata blob,
        uint256 statementPointOffset,
        uint256 initialOodPoint,
        uint256 round0OodPoint,
        uint256 round1OodPoint,
        uint256 round2OodPoint,
        uint256[] memory fullPoint
    )
        internal
        pure
        returns (
            uint256 statementEq,
            uint256 initialEq,
            uint256 round0Eq,
            uint256 round1Eq,
            uint256 round2Eq
        )
    {
        statementEq = KoalaBearExt5.ONE;
        initialEq = KoalaBearExt5.ONE;
        round0Eq = KoalaBearExt5.ONE;
        round1Eq = KoalaBearExt5.ONE;
        round2Eq = KoalaBearExt5.ONE;

        uint256 initialCurrent = initialOodPoint;
        uint256 round0Current = round0OodPoint;
        uint256 round1Current = round1OodPoint;
        uint256 round2Current = round2OodPoint;
        uint256 pointBase;
        assembly ("memory-safe") {
            pointBase := add(fullPoint, 0x20)
        }

        unchecked {
            {
                uint256 q;
                uint256 statementPointValue;
                assembly ("memory-safe") {
                    q := mload(add(pointBase, 672))
                    statementPointValue := and(
                        calldataload(add(add(blob.offset, statementPointOffset), 420)),
                        not(sub(shl(96, 1), 1))
                    )
                }
                statementEq = _eqTerm(statementPointValue, q);
                initialEq = _eqTerm(initialCurrent, q);
                round0Eq = _eqTerm(round0Current, q);
                round1Eq = _eqTerm(round1Current, q);
                round2Eq = _eqTerm(round2Current, q);
                initialCurrent = KoalaBearExt5.square(initialCurrent);
                round0Current = KoalaBearExt5.square(round0Current);
                round1Current = KoalaBearExt5.square(round1Current);
                round2Current = KoalaBearExt5.square(round2Current);
            }
            for (uint256 i = 21; i > 0; --i) {
                uint256 q;
                uint256 statementPointValue;
                assembly ("memory-safe") {
                    let idx := sub(i, 1)
                    q := mload(add(pointBase, shl(5, idx)))
                    statementPointValue := and(
                        calldataload(add(add(blob.offset, statementPointOffset), mul(20, idx))),
                        not(sub(shl(96, 1), 1))
                    )
                }

                statementEq = KoalaBearExt5.mul(statementEq, _eqTerm(statementPointValue, q));
                initialEq = KoalaBearExt5.mul(initialEq, _eqTerm(initialCurrent, q));
                initialCurrent = KoalaBearExt5.square(initialCurrent);

                if (i > 4) {
                    round0Eq = KoalaBearExt5.mul(round0Eq, _eqTerm(round0Current, q));
                    round0Current = KoalaBearExt5.square(round0Current);
                    if (i > 8) {
                        round1Eq = KoalaBearExt5.mul(round1Eq, _eqTerm(round1Current, q));
                        round1Current = KoalaBearExt5.square(round1Current);
                        if (i > 12) {
                            round2Eq = KoalaBearExt5.mul(round2Eq, _eqTerm(round2Current, q));
                            round2Current = KoalaBearExt5.square(round2Current);
                        }
                    }
                }
            }
        }
    }
}

contract KoalaBearEqPeelHarness {
    function mul(uint256 a, uint256 b) external pure returns (uint256) {
        return KoalaBearExt5.mul(a, b);
    }

    function square(uint256 a) external pure returns (uint256) {
        return KoalaBearExt5.square(a);
    }

    function eq(uint256 a, uint256 b) external pure returns (uint256) {
        return KoalaBearEqPeel._eqTerm(a, b);
    }

    function squares(uint256 a, uint256 n) external view returns (uint256 used, uint256 result) {
        uint256 start = gasleft();
        result = a;
        for (uint256 i; i < n; ++i) {
            result = KoalaBearExt5.square(result);
        }
        used = start - gasleft();
    }

    function prepare(bytes calldata statement, uint256[4] memory ood, uint256[] memory point)
        external
        view
        returns (uint256 used, uint256[5] memory result)
    {
        require(statement.length == 440 && point.length == 22, "SHAPE");
        uint256 start = gasleft();
        (result[0], result[1], result[2], result[3], result[4]) =
            KoalaBearEqPeel._evaluateFixedEqTermsBlobRaw(
                statement, 0, ood[0], ood[1], ood[2], ood[3], point
            );
        used = start - gasleft();
    }
}

library KoalaBearEqFused {
    function _eqTerm(uint256 p, uint256 q) internal pure returns (uint256) {
        uint256 out;
        assembly ("memory-safe") {
            let M := 0x7f000001
            let mask := 0xffffffff

            let p0 := shr(224, p)
            let p1 := and(shr(192, p), mask)
            let p2 := and(shr(160, p), mask)
            let p3 := and(shr(128, p), mask)
            let p4 := and(shr(96, p), mask)

            let q0 := shr(224, q)
            let q1 := and(shr(192, q), mask)
            let q2 := and(shr(160, q), mask)
            let q3 := and(shr(128, q), mask)
            let q4 := and(shr(96, q), mask)

            let c0 := mul(p0, q0)
            let c1 := add(mul(p0, q1), mul(p1, q0))
            let c2 := add(add(mul(p0, q2), mul(p1, q1)), mul(p2, q0))
            let c3 := add(add(add(mul(p0, q3), mul(p1, q2)), mul(p2, q1)), mul(p3, q0))
            let c4 :=
                add(add(add(add(mul(p0, q4), mul(p1, q3)), mul(p2, q2)), mul(p3, q1)), mul(p4, q0))
            let c5 := add(add(add(mul(p1, q4), mul(p2, q3)), mul(p3, q2)), mul(p4, q1))
            let c6 := add(add(mul(p2, q4), mul(p3, q3)), mul(p4, q2))
            let c7 := add(mul(p3, q4), mul(p4, q3))
            let c8 := mul(p4, q4)
            let bias := shl(35, M)

            let m0 := add(add(c0, c5), sub(bias, c8))
            let m1 := add(c1, c6)
            let m2 := add(add(add(c2, sub(bias, c5)), c7), c8)
            let m3 := add(add(c3, sub(bias, c6)), c8)
            let m4 := add(c4, sub(bias, c7))

            let b := shl(2, M)
            let o0 := mod(add(add(add(1, shl(1, m0)), b), sub(0, add(p0, q0))), M)
            let o1 := mod(add(add(shl(1, m1), b), sub(0, add(p1, q1))), M)
            let o2 := mod(add(add(shl(1, m2), b), sub(0, add(p2, q2))), M)
            let o3 := mod(add(add(shl(1, m3), b), sub(0, add(p3, q3))), M)
            let o4 := mod(add(add(shl(1, m4), b), sub(0, add(p4, q4))), M)

            out := or(
                or(or(shl(224, o0), shl(192, o1)), or(shl(160, o2), shl(128, o3))),
                shl(96, o4)
            )
        }
        return out;
    }

    function _evaluateFixedEqTermsBlobRaw(
        bytes calldata blob,
        uint256 statementPointOffset,
        uint256 initialOodPoint,
        uint256 round0OodPoint,
        uint256 round1OodPoint,
        uint256 round2OodPoint,
        uint256[] memory fullPoint
    )
        internal
        pure
        returns (
            uint256 statementEq,
            uint256 initialEq,
            uint256 round0Eq,
            uint256 round1Eq,
            uint256 round2Eq
        )
    {
        statementEq = KoalaBearExt5.ONE;
        initialEq = KoalaBearExt5.ONE;
        round0Eq = KoalaBearExt5.ONE;
        round1Eq = KoalaBearExt5.ONE;
        round2Eq = KoalaBearExt5.ONE;

        uint256 initialCurrent = initialOodPoint;
        uint256 round0Current = round0OodPoint;
        uint256 round1Current = round1OodPoint;
        uint256 round2Current = round2OodPoint;
        uint256 pointBase;
        assembly ("memory-safe") {
            pointBase := add(fullPoint, 0x20)
        }

        unchecked {
            for (uint256 i = 22; i > 0; --i) {
                uint256 q;
                uint256 statementPointValue;
                assembly ("memory-safe") {
                    let idx := sub(i, 1)
                    q := mload(add(pointBase, shl(5, idx)))
                    statementPointValue := and(
                        calldataload(add(add(blob.offset, statementPointOffset), mul(20, idx))),
                        not(sub(shl(96, 1), 1))
                    )
                }

                statementEq = KoalaBearExt5.mul(statementEq, _eqTerm(statementPointValue, q));
                initialEq = KoalaBearExt5.mul(initialEq, _eqTerm(initialCurrent, q));
                initialCurrent = KoalaBearExt5.square(initialCurrent);

                if (i > 4) {
                    round0Eq = KoalaBearExt5.mul(round0Eq, _eqTerm(round0Current, q));
                    round0Current = KoalaBearExt5.square(round0Current);
                    if (i > 8) {
                        round1Eq = KoalaBearExt5.mul(round1Eq, _eqTerm(round1Current, q));
                        round1Current = KoalaBearExt5.square(round1Current);
                        if (i > 12) {
                            round2Eq = KoalaBearExt5.mul(round2Eq, _eqTerm(round2Current, q));
                            round2Current = KoalaBearExt5.square(round2Current);
                        }
                    }
                }
            }
        }
    }
}

contract KoalaBearEqFusedHarness {
    function mul(uint256 a, uint256 b) external pure returns (uint256) {
        return KoalaBearExt5.mul(a, b);
    }

    function square(uint256 a) external pure returns (uint256) {
        return KoalaBearExt5.square(a);
    }

    function eq(uint256 a, uint256 b) external pure returns (uint256) {
        return KoalaBearEqFused._eqTerm(a, b);
    }

    function squares(uint256 a, uint256 n) external view returns (uint256 used, uint256 result) {
        uint256 start = gasleft();
        result = a;
        for (uint256 i; i < n; ++i) {
            result = KoalaBearExt5.square(result);
        }
        used = start - gasleft();
    }

    function prepare(bytes calldata statement, uint256[4] memory ood, uint256[] memory point)
        external
        view
        returns (uint256 used, uint256[5] memory result)
    {
        require(statement.length == 440 && point.length == 22, "SHAPE");
        uint256 start = gasleft();
        (result[0], result[1], result[2], result[3], result[4]) =
            KoalaBearEqFused._evaluateFixedEqTermsBlobRaw(
                statement, 0, ood[0], ood[1], ood[2], ood[3], point
            );
        used = start - gasleft();
    }
}

library KoalaBearEqCombined {
    function _eqTerm(uint256 p, uint256 q) internal pure returns (uint256) {
        uint256 out;
        assembly ("memory-safe") {
            let M := 0x7f000001
            let mask := 0xffffffff

            let p0 := shr(224, p)
            let p1 := and(shr(192, p), mask)
            let p2 := and(shr(160, p), mask)
            let p3 := and(shr(128, p), mask)
            let p4 := and(shr(96, p), mask)

            let q0 := shr(224, q)
            let q1 := and(shr(192, q), mask)
            let q2 := and(shr(160, q), mask)
            let q3 := and(shr(128, q), mask)
            let q4 := and(shr(96, q), mask)

            let c0 := mul(p0, q0)
            let c1 := add(mul(p0, q1), mul(p1, q0))
            let c2 := add(add(mul(p0, q2), mul(p1, q1)), mul(p2, q0))
            let c3 := add(add(add(mul(p0, q3), mul(p1, q2)), mul(p2, q1)), mul(p3, q0))
            let c4 :=
                add(add(add(add(mul(p0, q4), mul(p1, q3)), mul(p2, q2)), mul(p3, q1)), mul(p4, q0))
            let c5 := add(add(add(mul(p1, q4), mul(p2, q3)), mul(p3, q2)), mul(p4, q1))
            let c6 := add(add(mul(p2, q4), mul(p3, q3)), mul(p4, q2))
            let c7 := add(mul(p3, q4), mul(p4, q3))
            let c8 := mul(p4, q4)
            let bias := shl(35, M)

            let m0 := add(add(c0, c5), sub(bias, c8))
            let m1 := add(c1, c6)
            let m2 := add(add(add(c2, sub(bias, c5)), c7), c8)
            let m3 := add(add(c3, sub(bias, c6)), c8)
            let m4 := add(c4, sub(bias, c7))

            let b := shl(2, M)
            let o0 := mod(add(add(add(1, shl(1, m0)), b), sub(0, add(p0, q0))), M)
            let o1 := mod(add(add(shl(1, m1), b), sub(0, add(p1, q1))), M)
            let o2 := mod(add(add(shl(1, m2), b), sub(0, add(p2, q2))), M)
            let o3 := mod(add(add(shl(1, m3), b), sub(0, add(p3, q3))), M)
            let o4 := mod(add(add(shl(1, m4), b), sub(0, add(p4, q4))), M)

            out := or(
                or(or(shl(224, o0), shl(192, o1)), or(shl(160, o2), shl(128, o3))),
                shl(96, o4)
            )
        }
        return out;
    }

    function _evaluateFixedEqTermsBlobRaw(
        bytes calldata blob,
        uint256 statementPointOffset,
        uint256 initialOodPoint,
        uint256 round0OodPoint,
        uint256 round1OodPoint,
        uint256 round2OodPoint,
        uint256[] memory fullPoint
    )
        internal
        pure
        returns (
            uint256 statementEq,
            uint256 initialEq,
            uint256 round0Eq,
            uint256 round1Eq,
            uint256 round2Eq
        )
    {
        statementEq = KoalaBearExt5.ONE;
        initialEq = KoalaBearExt5.ONE;
        round0Eq = KoalaBearExt5.ONE;
        round1Eq = KoalaBearExt5.ONE;
        round2Eq = KoalaBearExt5.ONE;

        uint256 initialCurrent = initialOodPoint;
        uint256 round0Current = round0OodPoint;
        uint256 round1Current = round1OodPoint;
        uint256 round2Current = round2OodPoint;
        uint256 pointBase;
        assembly ("memory-safe") {
            pointBase := add(fullPoint, 0x20)
        }

        unchecked {
            {
                uint256 q;
                uint256 statementPointValue;
                assembly ("memory-safe") {
                    q := mload(add(pointBase, 672))
                    statementPointValue := and(
                        calldataload(add(add(blob.offset, statementPointOffset), 420)),
                        not(sub(shl(96, 1), 1))
                    )
                }
                statementEq = _eqTerm(statementPointValue, q);
                initialEq = _eqTerm(initialCurrent, q);
                round0Eq = _eqTerm(round0Current, q);
                round1Eq = _eqTerm(round1Current, q);
                round2Eq = _eqTerm(round2Current, q);
                initialCurrent = KoalaBearExt5.square(initialCurrent);
                round0Current = KoalaBearExt5.square(round0Current);
                round1Current = KoalaBearExt5.square(round1Current);
                round2Current = KoalaBearExt5.square(round2Current);
            }
            for (uint256 i = 21; i > 0; --i) {
                uint256 q;
                uint256 statementPointValue;
                assembly ("memory-safe") {
                    let idx := sub(i, 1)
                    q := mload(add(pointBase, shl(5, idx)))
                    statementPointValue := and(
                        calldataload(add(add(blob.offset, statementPointOffset), mul(20, idx))),
                        not(sub(shl(96, 1), 1))
                    )
                }

                statementEq = KoalaBearExt5.mul(statementEq, _eqTerm(statementPointValue, q));
                initialEq = KoalaBearExt5.mul(initialEq, _eqTerm(initialCurrent, q));
                initialCurrent = KoalaBearExt5.square(initialCurrent);

                if (i > 4) {
                    round0Eq = KoalaBearExt5.mul(round0Eq, _eqTerm(round0Current, q));
                    round0Current = KoalaBearExt5.square(round0Current);
                    if (i > 8) {
                        round1Eq = KoalaBearExt5.mul(round1Eq, _eqTerm(round1Current, q));
                        round1Current = KoalaBearExt5.square(round1Current);
                        if (i > 12) {
                            round2Eq = KoalaBearExt5.mul(round2Eq, _eqTerm(round2Current, q));
                            round2Current = KoalaBearExt5.square(round2Current);
                        }
                    }
                }
            }
        }
    }
}

contract KoalaBearEqCombinedHarness {
    function mul(uint256 a, uint256 b) external pure returns (uint256) {
        return KoalaBearExt5.mul(a, b);
    }

    function square(uint256 a) external pure returns (uint256) {
        return KoalaBearExt5.square(a);
    }

    function eq(uint256 a, uint256 b) external pure returns (uint256) {
        return KoalaBearEqCombined._eqTerm(a, b);
    }

    function squares(uint256 a, uint256 n) external view returns (uint256 used, uint256 result) {
        uint256 start = gasleft();
        result = a;
        for (uint256 i; i < n; ++i) {
            result = KoalaBearExt5.square(result);
        }
        used = start - gasleft();
    }

    function prepare(bytes calldata statement, uint256[4] memory ood, uint256[] memory point)
        external
        view
        returns (uint256 used, uint256[5] memory result)
    {
        require(statement.length == 440 && point.length == 22, "SHAPE");
        uint256 start = gasleft();
        (result[0], result[1], result[2], result[3], result[4]) =
            KoalaBearEqCombined._evaluateFixedEqTermsBlobRaw(
                statement, 0, ood[0], ood[1], ood[2], ood[3], point
            );
        used = start - gasleft();
    }
}

library KoalaBearEqPackedMul {
    function _eqTerm(uint256 p, uint256 q) internal pure returns (uint256) {
        uint256 out;
        assembly ("memory-safe") {
            let M := 0x7f000001
            let mask := 0xffffffff

            let p0 := shr(224, p)
            let p1 := and(shr(192, p), mask)
            let p2 := and(shr(160, p), mask)
            let p3 := and(shr(128, p), mask)
            let p4 := and(shr(96, p), mask)

            let q0 := shr(224, q)
            let q1 := and(shr(192, q), mask)
            let q2 := and(shr(160, q), mask)
            let q3 := and(shr(128, q), mask)
            let q4 := and(shr(96, q), mask)

            let c0 := mul(p0, q0)
            let c1 := add(mul(p0, q1), mul(p1, q0))
            let c2 := add(add(mul(p0, q2), mul(p1, q1)), mul(p2, q0))
            let c3 := add(add(add(mul(p0, q3), mul(p1, q2)), mul(p2, q1)), mul(p3, q0))
            let c4 :=
                add(add(add(add(mul(p0, q4), mul(p1, q3)), mul(p2, q2)), mul(p3, q1)), mul(p4, q0))
            let c5 := add(add(add(mul(p1, q4), mul(p2, q3)), mul(p3, q2)), mul(p4, q1))
            let c6 := add(add(mul(p2, q4), mul(p3, q3)), mul(p4, q2))
            let c7 := add(mul(p3, q4), mul(p4, q3))
            let c8 := mul(p4, q4)
            let bias := shl(35, M)

            let m0 := add(add(c0, c5), sub(bias, c8))
            let m1 := add(c1, c6)
            let m2 := add(add(add(c2, sub(bias, c5)), c7), c8)
            let m3 := add(add(c3, sub(bias, c6)), c8)
            let m4 := add(c4, sub(bias, c7))

            let b := shl(2, M)
            let o0 := mod(add(add(add(1, shl(1, m0)), b), sub(0, add(p0, q0))), M)
            let o1 := mod(add(add(shl(1, m1), b), sub(0, add(p1, q1))), M)
            let o2 := mod(add(add(shl(1, m2), b), sub(0, add(p2, q2))), M)
            let o3 := mod(add(add(shl(1, m3), b), sub(0, add(p3, q3))), M)
            let o4 := mod(add(add(shl(1, m4), b), sub(0, add(p4, q4))), M)

            out := or(
                or(or(shl(224, o0), shl(192, o1)), or(shl(160, o2), shl(128, o3))),
                shl(96, o4)
            )
        }
        return out;
    }

    function _evaluateFixedEqTermsBlobRaw(
        bytes calldata blob,
        uint256 statementPointOffset,
        uint256 initialOodPoint,
        uint256 round0OodPoint,
        uint256 round1OodPoint,
        uint256 round2OodPoint,
        uint256[] memory fullPoint
    )
        internal
        pure
        returns (
            uint256 statementEq,
            uint256 initialEq,
            uint256 round0Eq,
            uint256 round1Eq,
            uint256 round2Eq
        )
    {
        statementEq = KoalaBearExt5.ONE;
        initialEq = KoalaBearExt5.ONE;
        round0Eq = KoalaBearExt5.ONE;
        round1Eq = KoalaBearExt5.ONE;
        round2Eq = KoalaBearExt5.ONE;

        uint256 initialCurrent = initialOodPoint;
        uint256 round0Current = round0OodPoint;
        uint256 round1Current = round1OodPoint;
        uint256 round2Current = round2OodPoint;
        uint256 pointBase;
        assembly ("memory-safe") {
            pointBase := add(fullPoint, 0x20)
        }

        unchecked {
            {
                uint256 q;
                uint256 statementPointValue;
                assembly ("memory-safe") {
                    q := mload(add(pointBase, 672))
                    statementPointValue := and(
                        calldataload(add(add(blob.offset, statementPointOffset), 420)),
                        not(sub(shl(96, 1), 1))
                    )
                }
                statementEq = _eqTerm(statementPointValue, q);
                initialEq = _eqTerm(initialCurrent, q);
                round0Eq = _eqTerm(round0Current, q);
                round1Eq = _eqTerm(round1Current, q);
                round2Eq = _eqTerm(round2Current, q);
                initialCurrent = KoalaBearExt5.square(initialCurrent);
                round0Current = KoalaBearExt5.square(round0Current);
                round1Current = KoalaBearExt5.square(round1Current);
                round2Current = KoalaBearExt5.square(round2Current);
            }
            for (uint256 i = 21; i > 0; --i) {
                uint256 q;
                uint256 statementPointValue;
                assembly ("memory-safe") {
                    let idx := sub(i, 1)
                    q := mload(add(pointBase, shl(5, idx)))
                    statementPointValue := and(
                        calldataload(add(add(blob.offset, statementPointOffset), mul(20, idx))),
                        not(sub(shl(96, 1), 1))
                    )
                }

                statementEq = KoalaBearPackedField.mul(statementEq, _eqTerm(statementPointValue, q));
                initialEq = KoalaBearPackedField.mul(initialEq, _eqTerm(initialCurrent, q));
                initialCurrent = KoalaBearExt5.square(initialCurrent);

                if (i > 4) {
                    round0Eq = KoalaBearPackedField.mul(round0Eq, _eqTerm(round0Current, q));
                    round0Current = KoalaBearExt5.square(round0Current);
                    if (i > 8) {
                        round1Eq = KoalaBearPackedField.mul(round1Eq, _eqTerm(round1Current, q));
                        round1Current = KoalaBearExt5.square(round1Current);
                        if (i > 12) {
                            round2Eq = KoalaBearPackedField.mul(round2Eq, _eqTerm(round2Current, q));
                            round2Current = KoalaBearExt5.square(round2Current);
                        }
                    }
                }
            }
        }
    }
}

contract KoalaBearEqPackedMulHarness {
    function mul(uint256 a, uint256 b) external pure returns (uint256) {
        return KoalaBearPackedField.mul(a, b);
    }

    function square(uint256 a) external pure returns (uint256) {
        return KoalaBearExt5.square(a);
    }

    function eq(uint256 a, uint256 b) external pure returns (uint256) {
        return KoalaBearEqPackedMul._eqTerm(a, b);
    }

    function squares(uint256 a, uint256 n) external view returns (uint256 used, uint256 result) {
        uint256 start = gasleft();
        result = a;
        for (uint256 i; i < n; ++i) {
            result = KoalaBearExt5.square(result);
        }
        used = start - gasleft();
    }

    function prepare(bytes calldata statement, uint256[4] memory ood, uint256[] memory point)
        external
        view
        returns (uint256 used, uint256[5] memory result)
    {
        require(statement.length == 440 && point.length == 22, "SHAPE");
        uint256 start = gasleft();
        (result[0], result[1], result[2], result[3], result[4]) =
            KoalaBearEqPackedMul._evaluateFixedEqTermsBlobRaw(
                statement, 0, ood[0], ood[1], ood[2], ood[3], point
            );
        used = start - gasleft();
    }
}

library KoalaBearEqPackedSquare {
    function _eqTerm(uint256 p, uint256 q) internal pure returns (uint256) {
        uint256 out;
        assembly ("memory-safe") {
            let M := 0x7f000001
            let mask := 0xffffffff

            let p0 := shr(224, p)
            let p1 := and(shr(192, p), mask)
            let p2 := and(shr(160, p), mask)
            let p3 := and(shr(128, p), mask)
            let p4 := and(shr(96, p), mask)

            let q0 := shr(224, q)
            let q1 := and(shr(192, q), mask)
            let q2 := and(shr(160, q), mask)
            let q3 := and(shr(128, q), mask)
            let q4 := and(shr(96, q), mask)

            let c0 := mul(p0, q0)
            let c1 := add(mul(p0, q1), mul(p1, q0))
            let c2 := add(add(mul(p0, q2), mul(p1, q1)), mul(p2, q0))
            let c3 := add(add(add(mul(p0, q3), mul(p1, q2)), mul(p2, q1)), mul(p3, q0))
            let c4 :=
                add(add(add(add(mul(p0, q4), mul(p1, q3)), mul(p2, q2)), mul(p3, q1)), mul(p4, q0))
            let c5 := add(add(add(mul(p1, q4), mul(p2, q3)), mul(p3, q2)), mul(p4, q1))
            let c6 := add(add(mul(p2, q4), mul(p3, q3)), mul(p4, q2))
            let c7 := add(mul(p3, q4), mul(p4, q3))
            let c8 := mul(p4, q4)
            let bias := shl(35, M)

            let m0 := add(add(c0, c5), sub(bias, c8))
            let m1 := add(c1, c6)
            let m2 := add(add(add(c2, sub(bias, c5)), c7), c8)
            let m3 := add(add(c3, sub(bias, c6)), c8)
            let m4 := add(c4, sub(bias, c7))

            let b := shl(2, M)
            let o0 := mod(add(add(add(1, shl(1, m0)), b), sub(0, add(p0, q0))), M)
            let o1 := mod(add(add(shl(1, m1), b), sub(0, add(p1, q1))), M)
            let o2 := mod(add(add(shl(1, m2), b), sub(0, add(p2, q2))), M)
            let o3 := mod(add(add(shl(1, m3), b), sub(0, add(p3, q3))), M)
            let o4 := mod(add(add(shl(1, m4), b), sub(0, add(p4, q4))), M)

            out := or(
                or(or(shl(224, o0), shl(192, o1)), or(shl(160, o2), shl(128, o3))),
                shl(96, o4)
            )
        }
        return out;
    }

    function _evaluateFixedEqTermsBlobRaw(
        bytes calldata blob,
        uint256 statementPointOffset,
        uint256 initialOodPoint,
        uint256 round0OodPoint,
        uint256 round1OodPoint,
        uint256 round2OodPoint,
        uint256[] memory fullPoint
    )
        internal
        pure
        returns (
            uint256 statementEq,
            uint256 initialEq,
            uint256 round0Eq,
            uint256 round1Eq,
            uint256 round2Eq
        )
    {
        statementEq = KoalaBearExt5.ONE;
        initialEq = KoalaBearExt5.ONE;
        round0Eq = KoalaBearExt5.ONE;
        round1Eq = KoalaBearExt5.ONE;
        round2Eq = KoalaBearExt5.ONE;

        uint256 initialCurrent = initialOodPoint;
        uint256 round0Current = round0OodPoint;
        uint256 round1Current = round1OodPoint;
        uint256 round2Current = round2OodPoint;
        uint256 pointBase;
        assembly ("memory-safe") {
            pointBase := add(fullPoint, 0x20)
        }

        unchecked {
            {
                uint256 q;
                uint256 statementPointValue;
                assembly ("memory-safe") {
                    q := mload(add(pointBase, 672))
                    statementPointValue := and(
                        calldataload(add(add(blob.offset, statementPointOffset), 420)),
                        not(sub(shl(96, 1), 1))
                    )
                }
                statementEq = _eqTerm(statementPointValue, q);
                initialEq = _eqTerm(initialCurrent, q);
                round0Eq = _eqTerm(round0Current, q);
                round1Eq = _eqTerm(round1Current, q);
                round2Eq = _eqTerm(round2Current, q);
                initialCurrent = KoalaBearPackedField.square(initialCurrent);
                round0Current = KoalaBearPackedField.square(round0Current);
                round1Current = KoalaBearPackedField.square(round1Current);
                round2Current = KoalaBearPackedField.square(round2Current);
            }
            for (uint256 i = 21; i > 0; --i) {
                uint256 q;
                uint256 statementPointValue;
                assembly ("memory-safe") {
                    let idx := sub(i, 1)
                    q := mload(add(pointBase, shl(5, idx)))
                    statementPointValue := and(
                        calldataload(add(add(blob.offset, statementPointOffset), mul(20, idx))),
                        not(sub(shl(96, 1), 1))
                    )
                }

                statementEq = KoalaBearPackedField.mul(statementEq, _eqTerm(statementPointValue, q));
                initialEq = KoalaBearPackedField.mul(initialEq, _eqTerm(initialCurrent, q));
                initialCurrent = KoalaBearPackedField.square(initialCurrent);

                if (i > 4) {
                    round0Eq = KoalaBearPackedField.mul(round0Eq, _eqTerm(round0Current, q));
                    round0Current = KoalaBearPackedField.square(round0Current);
                    if (i > 8) {
                        round1Eq = KoalaBearPackedField.mul(round1Eq, _eqTerm(round1Current, q));
                        round1Current = KoalaBearPackedField.square(round1Current);
                        if (i > 12) {
                            round2Eq = KoalaBearPackedField.mul(round2Eq, _eqTerm(round2Current, q));
                            round2Current = KoalaBearPackedField.square(round2Current);
                        }
                    }
                }
            }
        }
    }
}

contract KoalaBearEqPackedSquareHarness {
    function mul(uint256 a, uint256 b) external pure returns (uint256) {
        return KoalaBearPackedField.mul(a, b);
    }

    function square(uint256 a) external pure returns (uint256) {
        return KoalaBearPackedField.square(a);
    }

    function eq(uint256 a, uint256 b) external pure returns (uint256) {
        return KoalaBearEqPackedSquare._eqTerm(a, b);
    }

    function squares(uint256 a, uint256 n) external view returns (uint256 used, uint256 result) {
        uint256 start = gasleft();
        result = a;
        for (uint256 i; i < n; ++i) {
            result = KoalaBearPackedField.square(result);
        }
        used = start - gasleft();
    }

    function prepare(bytes calldata statement, uint256[4] memory ood, uint256[] memory point)
        external
        view
        returns (uint256 used, uint256[5] memory result)
    {
        require(statement.length == 440 && point.length == 22, "SHAPE");
        uint256 start = gasleft();
        (result[0], result[1], result[2], result[3], result[4]) =
            KoalaBearEqPackedSquare._evaluateFixedEqTermsBlobRaw(
                statement, 0, ood[0], ood[1], ood[2], ood[3], point
            );
        used = start - gasleft();
    }
}

library KoalaBearEqPackedEq {
    function _eqTerm(uint256 a, uint256 b) internal pure returns (uint256) {
        return KoalaBearPackedField.eq(a, b);
    }

    function _evaluateFixedEqTermsBlobRaw(
        bytes calldata blob,
        uint256 statementPointOffset,
        uint256 initialOodPoint,
        uint256 round0OodPoint,
        uint256 round1OodPoint,
        uint256 round2OodPoint,
        uint256[] memory fullPoint
    )
        internal
        pure
        returns (
            uint256 statementEq,
            uint256 initialEq,
            uint256 round0Eq,
            uint256 round1Eq,
            uint256 round2Eq
        )
    {
        statementEq = KoalaBearExt5.ONE;
        initialEq = KoalaBearExt5.ONE;
        round0Eq = KoalaBearExt5.ONE;
        round1Eq = KoalaBearExt5.ONE;
        round2Eq = KoalaBearExt5.ONE;

        uint256 initialCurrent = initialOodPoint;
        uint256 round0Current = round0OodPoint;
        uint256 round1Current = round1OodPoint;
        uint256 round2Current = round2OodPoint;
        uint256 pointBase;
        assembly ("memory-safe") {
            pointBase := add(fullPoint, 0x20)
        }

        unchecked {
            {
                uint256 q;
                uint256 statementPointValue;
                assembly ("memory-safe") {
                    q := mload(add(pointBase, 672))
                    statementPointValue := and(
                        calldataload(add(add(blob.offset, statementPointOffset), 420)),
                        not(sub(shl(96, 1), 1))
                    )
                }
                statementEq = _eqTerm(statementPointValue, q);
                initialEq = _eqTerm(initialCurrent, q);
                round0Eq = _eqTerm(round0Current, q);
                round1Eq = _eqTerm(round1Current, q);
                round2Eq = _eqTerm(round2Current, q);
                initialCurrent = KoalaBearPackedField.square(initialCurrent);
                round0Current = KoalaBearPackedField.square(round0Current);
                round1Current = KoalaBearPackedField.square(round1Current);
                round2Current = KoalaBearPackedField.square(round2Current);
            }
            for (uint256 i = 21; i > 0; --i) {
                uint256 q;
                uint256 statementPointValue;
                assembly ("memory-safe") {
                    let idx := sub(i, 1)
                    q := mload(add(pointBase, shl(5, idx)))
                    statementPointValue := and(
                        calldataload(add(add(blob.offset, statementPointOffset), mul(20, idx))),
                        not(sub(shl(96, 1), 1))
                    )
                }

                statementEq = KoalaBearPackedField.mul(statementEq, _eqTerm(statementPointValue, q));
                initialEq = KoalaBearPackedField.mul(initialEq, _eqTerm(initialCurrent, q));
                initialCurrent = KoalaBearPackedField.square(initialCurrent);

                if (i > 4) {
                    round0Eq = KoalaBearPackedField.mul(round0Eq, _eqTerm(round0Current, q));
                    round0Current = KoalaBearPackedField.square(round0Current);
                    if (i > 8) {
                        round1Eq = KoalaBearPackedField.mul(round1Eq, _eqTerm(round1Current, q));
                        round1Current = KoalaBearPackedField.square(round1Current);
                        if (i > 12) {
                            round2Eq = KoalaBearPackedField.mul(round2Eq, _eqTerm(round2Current, q));
                            round2Current = KoalaBearPackedField.square(round2Current);
                        }
                    }
                }
            }
        }
    }
}

contract KoalaBearEqPackedEqHarness {
    function mul(uint256 a, uint256 b) external pure returns (uint256) {
        return KoalaBearPackedField.mul(a, b);
    }

    function square(uint256 a) external pure returns (uint256) {
        return KoalaBearPackedField.square(a);
    }

    function eq(uint256 a, uint256 b) external pure returns (uint256) {
        return KoalaBearEqPackedEq._eqTerm(a, b);
    }

    function squares(uint256 a, uint256 n) external view returns (uint256 used, uint256 result) {
        uint256 start = gasleft();
        result = a;
        for (uint256 i; i < n; ++i) {
            result = KoalaBearPackedField.square(result);
        }
        used = start - gasleft();
    }

    function prepare(bytes calldata statement, uint256[4] memory ood, uint256[] memory point)
        external
        view
        returns (uint256 used, uint256[5] memory result)
    {
        require(statement.length == 440 && point.length == 22, "SHAPE");
        uint256 start = gasleft();
        (result[0], result[1], result[2], result[3], result[4]) =
            KoalaBearEqPackedEq._evaluateFixedEqTermsBlobRaw(
                statement, 0, ood[0], ood[1], ood[2], ood[3], point
            );
        used = start - gasleft();
    }
}

library KoalaBearEqNoFinalSquares {
    function _eqTerm(uint256 a, uint256 b) internal pure returns (uint256) {
        return KoalaBearPackedField.eq(a, b);
    }

    function _evaluateFixedEqTermsBlobRaw(
        bytes calldata blob,
        uint256 statementPointOffset,
        uint256 initialOodPoint,
        uint256 round0OodPoint,
        uint256 round1OodPoint,
        uint256 round2OodPoint,
        uint256[] memory fullPoint
    )
        internal
        pure
        returns (
            uint256 statementEq,
            uint256 initialEq,
            uint256 round0Eq,
            uint256 round1Eq,
            uint256 round2Eq
        )
    {
        statementEq = KoalaBearExt5.ONE;
        initialEq = KoalaBearExt5.ONE;
        round0Eq = KoalaBearExt5.ONE;
        round1Eq = KoalaBearExt5.ONE;
        round2Eq = KoalaBearExt5.ONE;

        uint256 initialCurrent = initialOodPoint;
        uint256 round0Current = round0OodPoint;
        uint256 round1Current = round1OodPoint;
        uint256 round2Current = round2OodPoint;
        uint256 pointBase;
        assembly ("memory-safe") {
            pointBase := add(fullPoint, 0x20)
        }

        unchecked {
            {
                uint256 q;
                uint256 statementPointValue;
                assembly ("memory-safe") {
                    q := mload(add(pointBase, 672))
                    statementPointValue := and(
                        calldataload(add(add(blob.offset, statementPointOffset), 420)),
                        not(sub(shl(96, 1), 1))
                    )
                }
                statementEq = _eqTerm(statementPointValue, q);
                initialEq = _eqTerm(initialCurrent, q);
                round0Eq = _eqTerm(round0Current, q);
                round1Eq = _eqTerm(round1Current, q);
                round2Eq = _eqTerm(round2Current, q);
                initialCurrent = KoalaBearPackedField.square(initialCurrent);
                round0Current = KoalaBearPackedField.square(round0Current);
                round1Current = KoalaBearPackedField.square(round1Current);
                round2Current = KoalaBearPackedField.square(round2Current);
            }
            for (uint256 i = 21; i > 0; --i) {
                uint256 q;
                uint256 statementPointValue;
                assembly ("memory-safe") {
                    let idx := sub(i, 1)
                    q := mload(add(pointBase, shl(5, idx)))
                    statementPointValue := and(
                        calldataload(add(add(blob.offset, statementPointOffset), mul(20, idx))),
                        not(sub(shl(96, 1), 1))
                    )
                }

                statementEq = KoalaBearPackedField.mul(statementEq, _eqTerm(statementPointValue, q));
                initialEq = KoalaBearPackedField.mul(initialEq, _eqTerm(initialCurrent, q));
                if (i > 1) initialCurrent = KoalaBearPackedField.square(initialCurrent);

                if (i > 4) {
                    round0Eq = KoalaBearPackedField.mul(round0Eq, _eqTerm(round0Current, q));
                    if (i > 5) round0Current = KoalaBearPackedField.square(round0Current);
                    if (i > 8) {
                        round1Eq = KoalaBearPackedField.mul(round1Eq, _eqTerm(round1Current, q));
                        if (i > 9) round1Current = KoalaBearPackedField.square(round1Current);
                        if (i > 12) {
                            round2Eq = KoalaBearPackedField.mul(round2Eq, _eqTerm(round2Current, q));
                            if (i > 13) round2Current = KoalaBearPackedField.square(round2Current);
                        }
                    }
                }
            }
        }
    }
}

contract KoalaBearEqNoFinalSquaresHarness {
    function mul(uint256 a, uint256 b) external pure returns (uint256) {
        return KoalaBearPackedField.mul(a, b);
    }

    function square(uint256 a) external pure returns (uint256) {
        return KoalaBearPackedField.square(a);
    }

    function eq(uint256 a, uint256 b) external pure returns (uint256) {
        return KoalaBearEqNoFinalSquares._eqTerm(a, b);
    }

    function squares(uint256 a, uint256 n) external view returns (uint256 used, uint256 result) {
        uint256 start = gasleft();
        result = a;
        for (uint256 i; i < n; ++i) {
            result = KoalaBearPackedField.square(result);
        }
        used = start - gasleft();
    }

    function prepare(bytes calldata statement, uint256[4] memory ood, uint256[] memory point)
        external
        view
        returns (uint256 used, uint256[5] memory result)
    {
        require(statement.length == 440 && point.length == 22, "SHAPE");
        uint256 start = gasleft();
        (result[0], result[1], result[2], result[3], result[4]) =
            KoalaBearEqNoFinalSquares._evaluateFixedEqTermsBlobRaw(
                statement, 0, ood[0], ood[1], ood[2], ood[3], point
            );
        used = start - gasleft();
    }
}

library KoalaBearEqPackedMulNoFinal {
    function _eqTerm(uint256 p, uint256 q) internal pure returns (uint256) {
        uint256 out;
        assembly ("memory-safe") {
            let M := 0x7f000001
            let mask := 0xffffffff

            let p0 := shr(224, p)
            let p1 := and(shr(192, p), mask)
            let p2 := and(shr(160, p), mask)
            let p3 := and(shr(128, p), mask)
            let p4 := and(shr(96, p), mask)

            let q0 := shr(224, q)
            let q1 := and(shr(192, q), mask)
            let q2 := and(shr(160, q), mask)
            let q3 := and(shr(128, q), mask)
            let q4 := and(shr(96, q), mask)

            let c0 := mul(p0, q0)
            let c1 := add(mul(p0, q1), mul(p1, q0))
            let c2 := add(add(mul(p0, q2), mul(p1, q1)), mul(p2, q0))
            let c3 := add(add(add(mul(p0, q3), mul(p1, q2)), mul(p2, q1)), mul(p3, q0))
            let c4 :=
                add(add(add(add(mul(p0, q4), mul(p1, q3)), mul(p2, q2)), mul(p3, q1)), mul(p4, q0))
            let c5 := add(add(add(mul(p1, q4), mul(p2, q3)), mul(p3, q2)), mul(p4, q1))
            let c6 := add(add(mul(p2, q4), mul(p3, q3)), mul(p4, q2))
            let c7 := add(mul(p3, q4), mul(p4, q3))
            let c8 := mul(p4, q4)
            let bias := shl(35, M)

            let m0 := add(add(c0, c5), sub(bias, c8))
            let m1 := add(c1, c6)
            let m2 := add(add(add(c2, sub(bias, c5)), c7), c8)
            let m3 := add(add(c3, sub(bias, c6)), c8)
            let m4 := add(c4, sub(bias, c7))

            let b := shl(2, M)
            let o0 := mod(add(add(add(1, shl(1, m0)), b), sub(0, add(p0, q0))), M)
            let o1 := mod(add(add(shl(1, m1), b), sub(0, add(p1, q1))), M)
            let o2 := mod(add(add(shl(1, m2), b), sub(0, add(p2, q2))), M)
            let o3 := mod(add(add(shl(1, m3), b), sub(0, add(p3, q3))), M)
            let o4 := mod(add(add(shl(1, m4), b), sub(0, add(p4, q4))), M)

            out := or(
                or(or(shl(224, o0), shl(192, o1)), or(shl(160, o2), shl(128, o3))),
                shl(96, o4)
            )
        }
        return out;
    }

    function _evaluateFixedEqTermsBlobRaw(
        bytes calldata blob,
        uint256 statementPointOffset,
        uint256 initialOodPoint,
        uint256 round0OodPoint,
        uint256 round1OodPoint,
        uint256 round2OodPoint,
        uint256[] memory fullPoint
    )
        internal
        pure
        returns (
            uint256 statementEq,
            uint256 initialEq,
            uint256 round0Eq,
            uint256 round1Eq,
            uint256 round2Eq
        )
    {
        statementEq = KoalaBearExt5.ONE;
        initialEq = KoalaBearExt5.ONE;
        round0Eq = KoalaBearExt5.ONE;
        round1Eq = KoalaBearExt5.ONE;
        round2Eq = KoalaBearExt5.ONE;

        uint256 initialCurrent = initialOodPoint;
        uint256 round0Current = round0OodPoint;
        uint256 round1Current = round1OodPoint;
        uint256 round2Current = round2OodPoint;
        uint256 pointBase;
        assembly ("memory-safe") {
            pointBase := add(fullPoint, 0x20)
        }

        unchecked {
            {
                uint256 q;
                uint256 statementPointValue;
                assembly ("memory-safe") {
                    q := mload(add(pointBase, 672))
                    statementPointValue := and(
                        calldataload(add(add(blob.offset, statementPointOffset), 420)),
                        not(sub(shl(96, 1), 1))
                    )
                }
                statementEq = _eqTerm(statementPointValue, q);
                initialEq = _eqTerm(initialCurrent, q);
                round0Eq = _eqTerm(round0Current, q);
                round1Eq = _eqTerm(round1Current, q);
                round2Eq = _eqTerm(round2Current, q);
                initialCurrent = KoalaBearExt5.square(initialCurrent);
                round0Current = KoalaBearExt5.square(round0Current);
                round1Current = KoalaBearExt5.square(round1Current);
                round2Current = KoalaBearExt5.square(round2Current);
            }
            for (uint256 i = 21; i > 0; --i) {
                uint256 q;
                uint256 statementPointValue;
                assembly ("memory-safe") {
                    let idx := sub(i, 1)
                    q := mload(add(pointBase, shl(5, idx)))
                    statementPointValue := and(
                        calldataload(add(add(blob.offset, statementPointOffset), mul(20, idx))),
                        not(sub(shl(96, 1), 1))
                    )
                }

                statementEq = KoalaBearPackedField.mul(statementEq, _eqTerm(statementPointValue, q));
                initialEq = KoalaBearPackedField.mul(initialEq, _eqTerm(initialCurrent, q));
                if (i > 1) initialCurrent = KoalaBearExt5.square(initialCurrent);

                if (i > 4) {
                    round0Eq = KoalaBearPackedField.mul(round0Eq, _eqTerm(round0Current, q));
                    if (i > 5) round0Current = KoalaBearExt5.square(round0Current);
                    if (i > 8) {
                        round1Eq = KoalaBearPackedField.mul(round1Eq, _eqTerm(round1Current, q));
                        if (i > 9) round1Current = KoalaBearExt5.square(round1Current);
                        if (i > 12) {
                            round2Eq = KoalaBearPackedField.mul(round2Eq, _eqTerm(round2Current, q));
                            if (i > 13) round2Current = KoalaBearExt5.square(round2Current);
                        }
                    }
                }
            }
        }
    }
}

contract KoalaBearEqPackedMulNoFinalHarness {
    function mul(uint256 a, uint256 b) external pure returns (uint256) {
        return KoalaBearPackedField.mul(a, b);
    }

    function square(uint256 a) external pure returns (uint256) {
        return KoalaBearExt5.square(a);
    }

    function eq(uint256 a, uint256 b) external pure returns (uint256) {
        return KoalaBearEqPackedMulNoFinal._eqTerm(a, b);
    }

    function squares(uint256 a, uint256 n) external view returns (uint256 used, uint256 result) {
        uint256 start = gasleft();
        result = a;
        for (uint256 i; i < n; ++i) {
            result = KoalaBearExt5.square(result);
        }
        used = start - gasleft();
    }

    function prepare(bytes calldata statement, uint256[4] memory ood, uint256[] memory point)
        external
        view
        returns (uint256 used, uint256[5] memory result)
    {
        require(statement.length == 440 && point.length == 22, "SHAPE");
        uint256 start = gasleft();
        (result[0], result[1], result[2], result[3], result[4]) =
            KoalaBearEqPackedMulNoFinal._evaluateFixedEqTermsBlobRaw(
                statement, 0, ood[0], ood[1], ood[2], ood[3], point
            );
        used = start - gasleft();
    }
}

library BabyBearEqBaseline {
    function _eqTerm(uint256 p, uint256 q) internal pure returns (uint256) {
        uint256 out;
        assembly ("memory-safe") {
            let M := 0x78000001
            let mask := 0xffffffff

            let p0 := shr(224, p)
            let p1 := and(shr(192, p), mask)
            let p2 := and(shr(160, p), mask)
            let p3 := and(shr(128, p), mask)
            let p4 := and(shr(96, p), mask)

            let q0 := shr(224, q)
            let q1 := and(shr(192, q), mask)
            let q2 := and(shr(160, q), mask)
            let q3 := and(shr(128, q), mask)
            let q4 := and(shr(96, q), mask)

            let c0 := mul(p0, q0)
            let c1 := add(mul(p0, q1), mul(p1, q0))
            let c2 := add(add(mul(p0, q2), mul(p1, q1)), mul(p2, q0))
            let c3 := add(add(add(mul(p0, q3), mul(p1, q2)), mul(p2, q1)), mul(p3, q0))
            let c4 :=
                add(add(add(add(mul(p0, q4), mul(p1, q3)), mul(p2, q2)), mul(p3, q1)), mul(p4, q0))
            let c5 := add(add(add(mul(p1, q4), mul(p2, q3)), mul(p3, q2)), mul(p4, q1))
            let c6 := add(add(mul(p2, q4), mul(p3, q3)), mul(p4, q2))
            let c7 := add(mul(p3, q4), mul(p4, q3))
            let c8 := mul(p4, q4)
            let m0 := mod(add(c0, shl(1, c5)), M)
            let m1 := mod(add(c1, shl(1, c6)), M)
            let m2 := mod(add(c2, shl(1, c7)), M)
            let m3 := mod(add(c3, shl(1, c8)), M)
            let m4 := mod(c4, M)
            let b := shl(2, M)
            let o0 := mod(add(add(add(1, shl(1, m0)), b), sub(0, add(p0, q0))), M)
            let o1 := mod(add(add(shl(1, m1), b), sub(0, add(p1, q1))), M)
            let o2 := mod(add(add(shl(1, m2), b), sub(0, add(p2, q2))), M)
            let o3 := mod(add(add(shl(1, m3), b), sub(0, add(p3, q3))), M)
            let o4 := mod(add(add(shl(1, m4), b), sub(0, add(p4, q4))), M)

            out := or(
                or(or(shl(224, o0), shl(192, o1)), or(shl(160, o2), shl(128, o3))),
                shl(96, o4)
            )
        }
        return out;
    }

    function _evaluateFixedEqTermsBlobRaw(
        bytes calldata blob,
        uint256 statementPointOffset,
        uint256 initialOodPoint,
        uint256 round0OodPoint,
        uint256 round1OodPoint,
        uint256 round2OodPoint,
        uint256[] memory fullPoint
    )
        internal
        pure
        returns (
            uint256 statementEq,
            uint256 initialEq,
            uint256 round0Eq,
            uint256 round1Eq,
            uint256 round2Eq
        )
    {
        statementEq = BabyBearBenchField.ONE;
        initialEq = BabyBearBenchField.ONE;
        round0Eq = BabyBearBenchField.ONE;
        round1Eq = BabyBearBenchField.ONE;
        round2Eq = BabyBearBenchField.ONE;

        uint256 initialCurrent = initialOodPoint;
        uint256 round0Current = round0OodPoint;
        uint256 round1Current = round1OodPoint;
        uint256 round2Current = round2OodPoint;
        uint256 pointBase;
        assembly ("memory-safe") {
            pointBase := add(fullPoint, 0x20)
        }

        unchecked {
            for (uint256 i = 22; i > 0; --i) {
                uint256 q;
                uint256 statementPointValue;
                assembly ("memory-safe") {
                    let idx := sub(i, 1)
                    q := mload(add(pointBase, shl(5, idx)))
                    statementPointValue := and(
                        calldataload(add(add(blob.offset, statementPointOffset), mul(20, idx))),
                        not(sub(shl(96, 1), 1))
                    )
                }

                statementEq = BabyBearBenchField.mul(statementEq, _eqTerm(statementPointValue, q));
                initialEq = BabyBearBenchField.mul(initialEq, _eqTerm(initialCurrent, q));
                initialCurrent = BabyBearBenchField.square(initialCurrent);

                if (i > 4) {
                    round0Eq = BabyBearBenchField.mul(round0Eq, _eqTerm(round0Current, q));
                    round0Current = BabyBearBenchField.square(round0Current);
                    if (i > 8) {
                        round1Eq = BabyBearBenchField.mul(round1Eq, _eqTerm(round1Current, q));
                        round1Current = BabyBearBenchField.square(round1Current);
                        if (i > 12) {
                            round2Eq = BabyBearBenchField.mul(round2Eq, _eqTerm(round2Current, q));
                            round2Current = BabyBearBenchField.square(round2Current);
                        }
                    }
                }
            }
        }
    }
}

contract BabyBearEqBaselineHarness {
    function mul(uint256 a, uint256 b) external pure returns (uint256) {
        return BabyBearBenchField.mul(a, b);
    }

    function square(uint256 a) external pure returns (uint256) {
        return BabyBearBenchField.square(a);
    }

    function eq(uint256 a, uint256 b) external pure returns (uint256) {
        return BabyBearEqBaseline._eqTerm(a, b);
    }

    function squares(uint256 a, uint256 n) external view returns (uint256 used, uint256 result) {
        uint256 start = gasleft();
        result = a;
        for (uint256 i; i < n; ++i) {
            result = BabyBearBenchField.square(result);
        }
        used = start - gasleft();
    }

    function prepare(bytes calldata statement, uint256[4] memory ood, uint256[] memory point)
        external
        view
        returns (uint256 used, uint256[5] memory result)
    {
        require(statement.length == 440 && point.length == 22, "SHAPE");
        uint256 start = gasleft();
        (result[0], result[1], result[2], result[3], result[4]) =
            BabyBearEqBaseline._evaluateFixedEqTermsBlobRaw(
                statement, 0, ood[0], ood[1], ood[2], ood[3], point
            );
        used = start - gasleft();
    }
}

library BabyBearEqPeel {
    function _eqTerm(uint256 p, uint256 q) internal pure returns (uint256) {
        uint256 out;
        assembly ("memory-safe") {
            let M := 0x78000001
            let mask := 0xffffffff

            let p0 := shr(224, p)
            let p1 := and(shr(192, p), mask)
            let p2 := and(shr(160, p), mask)
            let p3 := and(shr(128, p), mask)
            let p4 := and(shr(96, p), mask)

            let q0 := shr(224, q)
            let q1 := and(shr(192, q), mask)
            let q2 := and(shr(160, q), mask)
            let q3 := and(shr(128, q), mask)
            let q4 := and(shr(96, q), mask)

            let c0 := mul(p0, q0)
            let c1 := add(mul(p0, q1), mul(p1, q0))
            let c2 := add(add(mul(p0, q2), mul(p1, q1)), mul(p2, q0))
            let c3 := add(add(add(mul(p0, q3), mul(p1, q2)), mul(p2, q1)), mul(p3, q0))
            let c4 :=
                add(add(add(add(mul(p0, q4), mul(p1, q3)), mul(p2, q2)), mul(p3, q1)), mul(p4, q0))
            let c5 := add(add(add(mul(p1, q4), mul(p2, q3)), mul(p3, q2)), mul(p4, q1))
            let c6 := add(add(mul(p2, q4), mul(p3, q3)), mul(p4, q2))
            let c7 := add(mul(p3, q4), mul(p4, q3))
            let c8 := mul(p4, q4)
            let m0 := mod(add(c0, shl(1, c5)), M)
            let m1 := mod(add(c1, shl(1, c6)), M)
            let m2 := mod(add(c2, shl(1, c7)), M)
            let m3 := mod(add(c3, shl(1, c8)), M)
            let m4 := mod(c4, M)
            let b := shl(2, M)
            let o0 := mod(add(add(add(1, shl(1, m0)), b), sub(0, add(p0, q0))), M)
            let o1 := mod(add(add(shl(1, m1), b), sub(0, add(p1, q1))), M)
            let o2 := mod(add(add(shl(1, m2), b), sub(0, add(p2, q2))), M)
            let o3 := mod(add(add(shl(1, m3), b), sub(0, add(p3, q3))), M)
            let o4 := mod(add(add(shl(1, m4), b), sub(0, add(p4, q4))), M)

            out := or(
                or(or(shl(224, o0), shl(192, o1)), or(shl(160, o2), shl(128, o3))),
                shl(96, o4)
            )
        }
        return out;
    }

    function _evaluateFixedEqTermsBlobRaw(
        bytes calldata blob,
        uint256 statementPointOffset,
        uint256 initialOodPoint,
        uint256 round0OodPoint,
        uint256 round1OodPoint,
        uint256 round2OodPoint,
        uint256[] memory fullPoint
    )
        internal
        pure
        returns (
            uint256 statementEq,
            uint256 initialEq,
            uint256 round0Eq,
            uint256 round1Eq,
            uint256 round2Eq
        )
    {
        statementEq = BabyBearBenchField.ONE;
        initialEq = BabyBearBenchField.ONE;
        round0Eq = BabyBearBenchField.ONE;
        round1Eq = BabyBearBenchField.ONE;
        round2Eq = BabyBearBenchField.ONE;

        uint256 initialCurrent = initialOodPoint;
        uint256 round0Current = round0OodPoint;
        uint256 round1Current = round1OodPoint;
        uint256 round2Current = round2OodPoint;
        uint256 pointBase;
        assembly ("memory-safe") {
            pointBase := add(fullPoint, 0x20)
        }

        unchecked {
            {
                uint256 q;
                uint256 statementPointValue;
                assembly ("memory-safe") {
                    q := mload(add(pointBase, 672))
                    statementPointValue := and(
                        calldataload(add(add(blob.offset, statementPointOffset), 420)),
                        not(sub(shl(96, 1), 1))
                    )
                }
                statementEq = _eqTerm(statementPointValue, q);
                initialEq = _eqTerm(initialCurrent, q);
                round0Eq = _eqTerm(round0Current, q);
                round1Eq = _eqTerm(round1Current, q);
                round2Eq = _eqTerm(round2Current, q);
                initialCurrent = BabyBearBenchField.square(initialCurrent);
                round0Current = BabyBearBenchField.square(round0Current);
                round1Current = BabyBearBenchField.square(round1Current);
                round2Current = BabyBearBenchField.square(round2Current);
            }
            for (uint256 i = 21; i > 0; --i) {
                uint256 q;
                uint256 statementPointValue;
                assembly ("memory-safe") {
                    let idx := sub(i, 1)
                    q := mload(add(pointBase, shl(5, idx)))
                    statementPointValue := and(
                        calldataload(add(add(blob.offset, statementPointOffset), mul(20, idx))),
                        not(sub(shl(96, 1), 1))
                    )
                }

                statementEq = BabyBearBenchField.mul(statementEq, _eqTerm(statementPointValue, q));
                initialEq = BabyBearBenchField.mul(initialEq, _eqTerm(initialCurrent, q));
                initialCurrent = BabyBearBenchField.square(initialCurrent);

                if (i > 4) {
                    round0Eq = BabyBearBenchField.mul(round0Eq, _eqTerm(round0Current, q));
                    round0Current = BabyBearBenchField.square(round0Current);
                    if (i > 8) {
                        round1Eq = BabyBearBenchField.mul(round1Eq, _eqTerm(round1Current, q));
                        round1Current = BabyBearBenchField.square(round1Current);
                        if (i > 12) {
                            round2Eq = BabyBearBenchField.mul(round2Eq, _eqTerm(round2Current, q));
                            round2Current = BabyBearBenchField.square(round2Current);
                        }
                    }
                }
            }
        }
    }
}

contract BabyBearEqPeelHarness {
    function mul(uint256 a, uint256 b) external pure returns (uint256) {
        return BabyBearBenchField.mul(a, b);
    }

    function square(uint256 a) external pure returns (uint256) {
        return BabyBearBenchField.square(a);
    }

    function eq(uint256 a, uint256 b) external pure returns (uint256) {
        return BabyBearEqPeel._eqTerm(a, b);
    }

    function squares(uint256 a, uint256 n) external view returns (uint256 used, uint256 result) {
        uint256 start = gasleft();
        result = a;
        for (uint256 i; i < n; ++i) {
            result = BabyBearBenchField.square(result);
        }
        used = start - gasleft();
    }

    function prepare(bytes calldata statement, uint256[4] memory ood, uint256[] memory point)
        external
        view
        returns (uint256 used, uint256[5] memory result)
    {
        require(statement.length == 440 && point.length == 22, "SHAPE");
        uint256 start = gasleft();
        (result[0], result[1], result[2], result[3], result[4]) =
            BabyBearEqPeel._evaluateFixedEqTermsBlobRaw(
                statement, 0, ood[0], ood[1], ood[2], ood[3], point
            );
        used = start - gasleft();
    }
}

library BabyBearEqFused {
    function _eqTerm(uint256 p, uint256 q) internal pure returns (uint256) {
        uint256 out;
        assembly ("memory-safe") {
            let M := 0x78000001
            let mask := 0xffffffff

            let p0 := shr(224, p)
            let p1 := and(shr(192, p), mask)
            let p2 := and(shr(160, p), mask)
            let p3 := and(shr(128, p), mask)
            let p4 := and(shr(96, p), mask)

            let q0 := shr(224, q)
            let q1 := and(shr(192, q), mask)
            let q2 := and(shr(160, q), mask)
            let q3 := and(shr(128, q), mask)
            let q4 := and(shr(96, q), mask)

            let c0 := mul(p0, q0)
            let c1 := add(mul(p0, q1), mul(p1, q0))
            let c2 := add(add(mul(p0, q2), mul(p1, q1)), mul(p2, q0))
            let c3 := add(add(add(mul(p0, q3), mul(p1, q2)), mul(p2, q1)), mul(p3, q0))
            let c4 :=
                add(add(add(add(mul(p0, q4), mul(p1, q3)), mul(p2, q2)), mul(p3, q1)), mul(p4, q0))
            let c5 := add(add(add(mul(p1, q4), mul(p2, q3)), mul(p3, q2)), mul(p4, q1))
            let c6 := add(add(mul(p2, q4), mul(p3, q3)), mul(p4, q2))
            let c7 := add(mul(p3, q4), mul(p4, q3))
            let c8 := mul(p4, q4)
            let m0 := add(c0, shl(1, c5))
            let m1 := add(c1, shl(1, c6))
            let m2 := add(c2, shl(1, c7))
            let m3 := add(c3, shl(1, c8))
            let m4 := c4
            let b := shl(2, M)
            let o0 := mod(add(add(add(1, shl(1, m0)), b), sub(0, add(p0, q0))), M)
            let o1 := mod(add(add(shl(1, m1), b), sub(0, add(p1, q1))), M)
            let o2 := mod(add(add(shl(1, m2), b), sub(0, add(p2, q2))), M)
            let o3 := mod(add(add(shl(1, m3), b), sub(0, add(p3, q3))), M)
            let o4 := mod(add(add(shl(1, m4), b), sub(0, add(p4, q4))), M)

            out := or(
                or(or(shl(224, o0), shl(192, o1)), or(shl(160, o2), shl(128, o3))),
                shl(96, o4)
            )
        }
        return out;
    }

    function _evaluateFixedEqTermsBlobRaw(
        bytes calldata blob,
        uint256 statementPointOffset,
        uint256 initialOodPoint,
        uint256 round0OodPoint,
        uint256 round1OodPoint,
        uint256 round2OodPoint,
        uint256[] memory fullPoint
    )
        internal
        pure
        returns (
            uint256 statementEq,
            uint256 initialEq,
            uint256 round0Eq,
            uint256 round1Eq,
            uint256 round2Eq
        )
    {
        statementEq = BabyBearBenchField.ONE;
        initialEq = BabyBearBenchField.ONE;
        round0Eq = BabyBearBenchField.ONE;
        round1Eq = BabyBearBenchField.ONE;
        round2Eq = BabyBearBenchField.ONE;

        uint256 initialCurrent = initialOodPoint;
        uint256 round0Current = round0OodPoint;
        uint256 round1Current = round1OodPoint;
        uint256 round2Current = round2OodPoint;
        uint256 pointBase;
        assembly ("memory-safe") {
            pointBase := add(fullPoint, 0x20)
        }

        unchecked {
            for (uint256 i = 22; i > 0; --i) {
                uint256 q;
                uint256 statementPointValue;
                assembly ("memory-safe") {
                    let idx := sub(i, 1)
                    q := mload(add(pointBase, shl(5, idx)))
                    statementPointValue := and(
                        calldataload(add(add(blob.offset, statementPointOffset), mul(20, idx))),
                        not(sub(shl(96, 1), 1))
                    )
                }

                statementEq = BabyBearBenchField.mul(statementEq, _eqTerm(statementPointValue, q));
                initialEq = BabyBearBenchField.mul(initialEq, _eqTerm(initialCurrent, q));
                initialCurrent = BabyBearBenchField.square(initialCurrent);

                if (i > 4) {
                    round0Eq = BabyBearBenchField.mul(round0Eq, _eqTerm(round0Current, q));
                    round0Current = BabyBearBenchField.square(round0Current);
                    if (i > 8) {
                        round1Eq = BabyBearBenchField.mul(round1Eq, _eqTerm(round1Current, q));
                        round1Current = BabyBearBenchField.square(round1Current);
                        if (i > 12) {
                            round2Eq = BabyBearBenchField.mul(round2Eq, _eqTerm(round2Current, q));
                            round2Current = BabyBearBenchField.square(round2Current);
                        }
                    }
                }
            }
        }
    }
}

contract BabyBearEqFusedHarness {
    function mul(uint256 a, uint256 b) external pure returns (uint256) {
        return BabyBearBenchField.mul(a, b);
    }

    function square(uint256 a) external pure returns (uint256) {
        return BabyBearBenchField.square(a);
    }

    function eq(uint256 a, uint256 b) external pure returns (uint256) {
        return BabyBearEqFused._eqTerm(a, b);
    }

    function squares(uint256 a, uint256 n) external view returns (uint256 used, uint256 result) {
        uint256 start = gasleft();
        result = a;
        for (uint256 i; i < n; ++i) {
            result = BabyBearBenchField.square(result);
        }
        used = start - gasleft();
    }

    function prepare(bytes calldata statement, uint256[4] memory ood, uint256[] memory point)
        external
        view
        returns (uint256 used, uint256[5] memory result)
    {
        require(statement.length == 440 && point.length == 22, "SHAPE");
        uint256 start = gasleft();
        (result[0], result[1], result[2], result[3], result[4]) =
            BabyBearEqFused._evaluateFixedEqTermsBlobRaw(
                statement, 0, ood[0], ood[1], ood[2], ood[3], point
            );
        used = start - gasleft();
    }
}

library BabyBearEqCombined {
    function _eqTerm(uint256 p, uint256 q) internal pure returns (uint256) {
        uint256 out;
        assembly ("memory-safe") {
            let M := 0x78000001
            let mask := 0xffffffff

            let p0 := shr(224, p)
            let p1 := and(shr(192, p), mask)
            let p2 := and(shr(160, p), mask)
            let p3 := and(shr(128, p), mask)
            let p4 := and(shr(96, p), mask)

            let q0 := shr(224, q)
            let q1 := and(shr(192, q), mask)
            let q2 := and(shr(160, q), mask)
            let q3 := and(shr(128, q), mask)
            let q4 := and(shr(96, q), mask)

            let c0 := mul(p0, q0)
            let c1 := add(mul(p0, q1), mul(p1, q0))
            let c2 := add(add(mul(p0, q2), mul(p1, q1)), mul(p2, q0))
            let c3 := add(add(add(mul(p0, q3), mul(p1, q2)), mul(p2, q1)), mul(p3, q0))
            let c4 :=
                add(add(add(add(mul(p0, q4), mul(p1, q3)), mul(p2, q2)), mul(p3, q1)), mul(p4, q0))
            let c5 := add(add(add(mul(p1, q4), mul(p2, q3)), mul(p3, q2)), mul(p4, q1))
            let c6 := add(add(mul(p2, q4), mul(p3, q3)), mul(p4, q2))
            let c7 := add(mul(p3, q4), mul(p4, q3))
            let c8 := mul(p4, q4)
            let m0 := add(c0, shl(1, c5))
            let m1 := add(c1, shl(1, c6))
            let m2 := add(c2, shl(1, c7))
            let m3 := add(c3, shl(1, c8))
            let m4 := c4
            let b := shl(2, M)
            let o0 := mod(add(add(add(1, shl(1, m0)), b), sub(0, add(p0, q0))), M)
            let o1 := mod(add(add(shl(1, m1), b), sub(0, add(p1, q1))), M)
            let o2 := mod(add(add(shl(1, m2), b), sub(0, add(p2, q2))), M)
            let o3 := mod(add(add(shl(1, m3), b), sub(0, add(p3, q3))), M)
            let o4 := mod(add(add(shl(1, m4), b), sub(0, add(p4, q4))), M)

            out := or(
                or(or(shl(224, o0), shl(192, o1)), or(shl(160, o2), shl(128, o3))),
                shl(96, o4)
            )
        }
        return out;
    }

    function _evaluateFixedEqTermsBlobRaw(
        bytes calldata blob,
        uint256 statementPointOffset,
        uint256 initialOodPoint,
        uint256 round0OodPoint,
        uint256 round1OodPoint,
        uint256 round2OodPoint,
        uint256[] memory fullPoint
    )
        internal
        pure
        returns (
            uint256 statementEq,
            uint256 initialEq,
            uint256 round0Eq,
            uint256 round1Eq,
            uint256 round2Eq
        )
    {
        statementEq = BabyBearBenchField.ONE;
        initialEq = BabyBearBenchField.ONE;
        round0Eq = BabyBearBenchField.ONE;
        round1Eq = BabyBearBenchField.ONE;
        round2Eq = BabyBearBenchField.ONE;

        uint256 initialCurrent = initialOodPoint;
        uint256 round0Current = round0OodPoint;
        uint256 round1Current = round1OodPoint;
        uint256 round2Current = round2OodPoint;
        uint256 pointBase;
        assembly ("memory-safe") {
            pointBase := add(fullPoint, 0x20)
        }

        unchecked {
            {
                uint256 q;
                uint256 statementPointValue;
                assembly ("memory-safe") {
                    q := mload(add(pointBase, 672))
                    statementPointValue := and(
                        calldataload(add(add(blob.offset, statementPointOffset), 420)),
                        not(sub(shl(96, 1), 1))
                    )
                }
                statementEq = _eqTerm(statementPointValue, q);
                initialEq = _eqTerm(initialCurrent, q);
                round0Eq = _eqTerm(round0Current, q);
                round1Eq = _eqTerm(round1Current, q);
                round2Eq = _eqTerm(round2Current, q);
                initialCurrent = BabyBearBenchField.square(initialCurrent);
                round0Current = BabyBearBenchField.square(round0Current);
                round1Current = BabyBearBenchField.square(round1Current);
                round2Current = BabyBearBenchField.square(round2Current);
            }
            for (uint256 i = 21; i > 0; --i) {
                uint256 q;
                uint256 statementPointValue;
                assembly ("memory-safe") {
                    let idx := sub(i, 1)
                    q := mload(add(pointBase, shl(5, idx)))
                    statementPointValue := and(
                        calldataload(add(add(blob.offset, statementPointOffset), mul(20, idx))),
                        not(sub(shl(96, 1), 1))
                    )
                }

                statementEq = BabyBearBenchField.mul(statementEq, _eqTerm(statementPointValue, q));
                initialEq = BabyBearBenchField.mul(initialEq, _eqTerm(initialCurrent, q));
                initialCurrent = BabyBearBenchField.square(initialCurrent);

                if (i > 4) {
                    round0Eq = BabyBearBenchField.mul(round0Eq, _eqTerm(round0Current, q));
                    round0Current = BabyBearBenchField.square(round0Current);
                    if (i > 8) {
                        round1Eq = BabyBearBenchField.mul(round1Eq, _eqTerm(round1Current, q));
                        round1Current = BabyBearBenchField.square(round1Current);
                        if (i > 12) {
                            round2Eq = BabyBearBenchField.mul(round2Eq, _eqTerm(round2Current, q));
                            round2Current = BabyBearBenchField.square(round2Current);
                        }
                    }
                }
            }
        }
    }
}

contract BabyBearEqCombinedHarness {
    function mul(uint256 a, uint256 b) external pure returns (uint256) {
        return BabyBearBenchField.mul(a, b);
    }

    function square(uint256 a) external pure returns (uint256) {
        return BabyBearBenchField.square(a);
    }

    function eq(uint256 a, uint256 b) external pure returns (uint256) {
        return BabyBearEqCombined._eqTerm(a, b);
    }

    function squares(uint256 a, uint256 n) external view returns (uint256 used, uint256 result) {
        uint256 start = gasleft();
        result = a;
        for (uint256 i; i < n; ++i) {
            result = BabyBearBenchField.square(result);
        }
        used = start - gasleft();
    }

    function prepare(bytes calldata statement, uint256[4] memory ood, uint256[] memory point)
        external
        view
        returns (uint256 used, uint256[5] memory result)
    {
        require(statement.length == 440 && point.length == 22, "SHAPE");
        uint256 start = gasleft();
        (result[0], result[1], result[2], result[3], result[4]) =
            BabyBearEqCombined._evaluateFixedEqTermsBlobRaw(
                statement, 0, ood[0], ood[1], ood[2], ood[3], point
            );
        used = start - gasleft();
    }
}

library BabyBearEqPackedMul {
    function _eqTerm(uint256 p, uint256 q) internal pure returns (uint256) {
        uint256 out;
        assembly ("memory-safe") {
            let M := 0x78000001
            let mask := 0xffffffff

            let p0 := shr(224, p)
            let p1 := and(shr(192, p), mask)
            let p2 := and(shr(160, p), mask)
            let p3 := and(shr(128, p), mask)
            let p4 := and(shr(96, p), mask)

            let q0 := shr(224, q)
            let q1 := and(shr(192, q), mask)
            let q2 := and(shr(160, q), mask)
            let q3 := and(shr(128, q), mask)
            let q4 := and(shr(96, q), mask)

            let c0 := mul(p0, q0)
            let c1 := add(mul(p0, q1), mul(p1, q0))
            let c2 := add(add(mul(p0, q2), mul(p1, q1)), mul(p2, q0))
            let c3 := add(add(add(mul(p0, q3), mul(p1, q2)), mul(p2, q1)), mul(p3, q0))
            let c4 :=
                add(add(add(add(mul(p0, q4), mul(p1, q3)), mul(p2, q2)), mul(p3, q1)), mul(p4, q0))
            let c5 := add(add(add(mul(p1, q4), mul(p2, q3)), mul(p3, q2)), mul(p4, q1))
            let c6 := add(add(mul(p2, q4), mul(p3, q3)), mul(p4, q2))
            let c7 := add(mul(p3, q4), mul(p4, q3))
            let c8 := mul(p4, q4)
            let m0 := add(c0, shl(1, c5))
            let m1 := add(c1, shl(1, c6))
            let m2 := add(c2, shl(1, c7))
            let m3 := add(c3, shl(1, c8))
            let m4 := c4
            let b := shl(2, M)
            let o0 := mod(add(add(add(1, shl(1, m0)), b), sub(0, add(p0, q0))), M)
            let o1 := mod(add(add(shl(1, m1), b), sub(0, add(p1, q1))), M)
            let o2 := mod(add(add(shl(1, m2), b), sub(0, add(p2, q2))), M)
            let o3 := mod(add(add(shl(1, m3), b), sub(0, add(p3, q3))), M)
            let o4 := mod(add(add(shl(1, m4), b), sub(0, add(p4, q4))), M)

            out := or(
                or(or(shl(224, o0), shl(192, o1)), or(shl(160, o2), shl(128, o3))),
                shl(96, o4)
            )
        }
        return out;
    }

    function _evaluateFixedEqTermsBlobRaw(
        bytes calldata blob,
        uint256 statementPointOffset,
        uint256 initialOodPoint,
        uint256 round0OodPoint,
        uint256 round1OodPoint,
        uint256 round2OodPoint,
        uint256[] memory fullPoint
    )
        internal
        pure
        returns (
            uint256 statementEq,
            uint256 initialEq,
            uint256 round0Eq,
            uint256 round1Eq,
            uint256 round2Eq
        )
    {
        statementEq = BabyBearBenchField.ONE;
        initialEq = BabyBearBenchField.ONE;
        round0Eq = BabyBearBenchField.ONE;
        round1Eq = BabyBearBenchField.ONE;
        round2Eq = BabyBearBenchField.ONE;

        uint256 initialCurrent = initialOodPoint;
        uint256 round0Current = round0OodPoint;
        uint256 round1Current = round1OodPoint;
        uint256 round2Current = round2OodPoint;
        uint256 pointBase;
        assembly ("memory-safe") {
            pointBase := add(fullPoint, 0x20)
        }

        unchecked {
            {
                uint256 q;
                uint256 statementPointValue;
                assembly ("memory-safe") {
                    q := mload(add(pointBase, 672))
                    statementPointValue := and(
                        calldataload(add(add(blob.offset, statementPointOffset), 420)),
                        not(sub(shl(96, 1), 1))
                    )
                }
                statementEq = _eqTerm(statementPointValue, q);
                initialEq = _eqTerm(initialCurrent, q);
                round0Eq = _eqTerm(round0Current, q);
                round1Eq = _eqTerm(round1Current, q);
                round2Eq = _eqTerm(round2Current, q);
                initialCurrent = BabyBearBenchField.square(initialCurrent);
                round0Current = BabyBearBenchField.square(round0Current);
                round1Current = BabyBearBenchField.square(round1Current);
                round2Current = BabyBearBenchField.square(round2Current);
            }
            for (uint256 i = 21; i > 0; --i) {
                uint256 q;
                uint256 statementPointValue;
                assembly ("memory-safe") {
                    let idx := sub(i, 1)
                    q := mload(add(pointBase, shl(5, idx)))
                    statementPointValue := and(
                        calldataload(add(add(blob.offset, statementPointOffset), mul(20, idx))),
                        not(sub(shl(96, 1), 1))
                    )
                }

                statementEq = BabyBearPackedField.mul(statementEq, _eqTerm(statementPointValue, q));
                initialEq = BabyBearPackedField.mul(initialEq, _eqTerm(initialCurrent, q));
                initialCurrent = BabyBearBenchField.square(initialCurrent);

                if (i > 4) {
                    round0Eq = BabyBearPackedField.mul(round0Eq, _eqTerm(round0Current, q));
                    round0Current = BabyBearBenchField.square(round0Current);
                    if (i > 8) {
                        round1Eq = BabyBearPackedField.mul(round1Eq, _eqTerm(round1Current, q));
                        round1Current = BabyBearBenchField.square(round1Current);
                        if (i > 12) {
                            round2Eq = BabyBearPackedField.mul(round2Eq, _eqTerm(round2Current, q));
                            round2Current = BabyBearBenchField.square(round2Current);
                        }
                    }
                }
            }
        }
    }
}

contract BabyBearEqPackedMulHarness {
    function mul(uint256 a, uint256 b) external pure returns (uint256) {
        return BabyBearPackedField.mul(a, b);
    }

    function square(uint256 a) external pure returns (uint256) {
        return BabyBearBenchField.square(a);
    }

    function eq(uint256 a, uint256 b) external pure returns (uint256) {
        return BabyBearEqPackedMul._eqTerm(a, b);
    }

    function squares(uint256 a, uint256 n) external view returns (uint256 used, uint256 result) {
        uint256 start = gasleft();
        result = a;
        for (uint256 i; i < n; ++i) {
            result = BabyBearBenchField.square(result);
        }
        used = start - gasleft();
    }

    function prepare(bytes calldata statement, uint256[4] memory ood, uint256[] memory point)
        external
        view
        returns (uint256 used, uint256[5] memory result)
    {
        require(statement.length == 440 && point.length == 22, "SHAPE");
        uint256 start = gasleft();
        (result[0], result[1], result[2], result[3], result[4]) =
            BabyBearEqPackedMul._evaluateFixedEqTermsBlobRaw(
                statement, 0, ood[0], ood[1], ood[2], ood[3], point
            );
        used = start - gasleft();
    }
}

library BabyBearEqPackedSquare {
    function _eqTerm(uint256 p, uint256 q) internal pure returns (uint256) {
        uint256 out;
        assembly ("memory-safe") {
            let M := 0x78000001
            let mask := 0xffffffff

            let p0 := shr(224, p)
            let p1 := and(shr(192, p), mask)
            let p2 := and(shr(160, p), mask)
            let p3 := and(shr(128, p), mask)
            let p4 := and(shr(96, p), mask)

            let q0 := shr(224, q)
            let q1 := and(shr(192, q), mask)
            let q2 := and(shr(160, q), mask)
            let q3 := and(shr(128, q), mask)
            let q4 := and(shr(96, q), mask)

            let c0 := mul(p0, q0)
            let c1 := add(mul(p0, q1), mul(p1, q0))
            let c2 := add(add(mul(p0, q2), mul(p1, q1)), mul(p2, q0))
            let c3 := add(add(add(mul(p0, q3), mul(p1, q2)), mul(p2, q1)), mul(p3, q0))
            let c4 :=
                add(add(add(add(mul(p0, q4), mul(p1, q3)), mul(p2, q2)), mul(p3, q1)), mul(p4, q0))
            let c5 := add(add(add(mul(p1, q4), mul(p2, q3)), mul(p3, q2)), mul(p4, q1))
            let c6 := add(add(mul(p2, q4), mul(p3, q3)), mul(p4, q2))
            let c7 := add(mul(p3, q4), mul(p4, q3))
            let c8 := mul(p4, q4)
            let m0 := add(c0, shl(1, c5))
            let m1 := add(c1, shl(1, c6))
            let m2 := add(c2, shl(1, c7))
            let m3 := add(c3, shl(1, c8))
            let m4 := c4
            let b := shl(2, M)
            let o0 := mod(add(add(add(1, shl(1, m0)), b), sub(0, add(p0, q0))), M)
            let o1 := mod(add(add(shl(1, m1), b), sub(0, add(p1, q1))), M)
            let o2 := mod(add(add(shl(1, m2), b), sub(0, add(p2, q2))), M)
            let o3 := mod(add(add(shl(1, m3), b), sub(0, add(p3, q3))), M)
            let o4 := mod(add(add(shl(1, m4), b), sub(0, add(p4, q4))), M)

            out := or(
                or(or(shl(224, o0), shl(192, o1)), or(shl(160, o2), shl(128, o3))),
                shl(96, o4)
            )
        }
        return out;
    }

    function _evaluateFixedEqTermsBlobRaw(
        bytes calldata blob,
        uint256 statementPointOffset,
        uint256 initialOodPoint,
        uint256 round0OodPoint,
        uint256 round1OodPoint,
        uint256 round2OodPoint,
        uint256[] memory fullPoint
    )
        internal
        pure
        returns (
            uint256 statementEq,
            uint256 initialEq,
            uint256 round0Eq,
            uint256 round1Eq,
            uint256 round2Eq
        )
    {
        statementEq = BabyBearBenchField.ONE;
        initialEq = BabyBearBenchField.ONE;
        round0Eq = BabyBearBenchField.ONE;
        round1Eq = BabyBearBenchField.ONE;
        round2Eq = BabyBearBenchField.ONE;

        uint256 initialCurrent = initialOodPoint;
        uint256 round0Current = round0OodPoint;
        uint256 round1Current = round1OodPoint;
        uint256 round2Current = round2OodPoint;
        uint256 pointBase;
        assembly ("memory-safe") {
            pointBase := add(fullPoint, 0x20)
        }

        unchecked {
            {
                uint256 q;
                uint256 statementPointValue;
                assembly ("memory-safe") {
                    q := mload(add(pointBase, 672))
                    statementPointValue := and(
                        calldataload(add(add(blob.offset, statementPointOffset), 420)),
                        not(sub(shl(96, 1), 1))
                    )
                }
                statementEq = _eqTerm(statementPointValue, q);
                initialEq = _eqTerm(initialCurrent, q);
                round0Eq = _eqTerm(round0Current, q);
                round1Eq = _eqTerm(round1Current, q);
                round2Eq = _eqTerm(round2Current, q);
                initialCurrent = BabyBearPackedField.square(initialCurrent);
                round0Current = BabyBearPackedField.square(round0Current);
                round1Current = BabyBearPackedField.square(round1Current);
                round2Current = BabyBearPackedField.square(round2Current);
            }
            for (uint256 i = 21; i > 0; --i) {
                uint256 q;
                uint256 statementPointValue;
                assembly ("memory-safe") {
                    let idx := sub(i, 1)
                    q := mload(add(pointBase, shl(5, idx)))
                    statementPointValue := and(
                        calldataload(add(add(blob.offset, statementPointOffset), mul(20, idx))),
                        not(sub(shl(96, 1), 1))
                    )
                }

                statementEq = BabyBearPackedField.mul(statementEq, _eqTerm(statementPointValue, q));
                initialEq = BabyBearPackedField.mul(initialEq, _eqTerm(initialCurrent, q));
                initialCurrent = BabyBearPackedField.square(initialCurrent);

                if (i > 4) {
                    round0Eq = BabyBearPackedField.mul(round0Eq, _eqTerm(round0Current, q));
                    round0Current = BabyBearPackedField.square(round0Current);
                    if (i > 8) {
                        round1Eq = BabyBearPackedField.mul(round1Eq, _eqTerm(round1Current, q));
                        round1Current = BabyBearPackedField.square(round1Current);
                        if (i > 12) {
                            round2Eq = BabyBearPackedField.mul(round2Eq, _eqTerm(round2Current, q));
                            round2Current = BabyBearPackedField.square(round2Current);
                        }
                    }
                }
            }
        }
    }
}

contract BabyBearEqPackedSquareHarness {
    function mul(uint256 a, uint256 b) external pure returns (uint256) {
        return BabyBearPackedField.mul(a, b);
    }

    function square(uint256 a) external pure returns (uint256) {
        return BabyBearPackedField.square(a);
    }

    function eq(uint256 a, uint256 b) external pure returns (uint256) {
        return BabyBearEqPackedSquare._eqTerm(a, b);
    }

    function squares(uint256 a, uint256 n) external view returns (uint256 used, uint256 result) {
        uint256 start = gasleft();
        result = a;
        for (uint256 i; i < n; ++i) {
            result = BabyBearPackedField.square(result);
        }
        used = start - gasleft();
    }

    function prepare(bytes calldata statement, uint256[4] memory ood, uint256[] memory point)
        external
        view
        returns (uint256 used, uint256[5] memory result)
    {
        require(statement.length == 440 && point.length == 22, "SHAPE");
        uint256 start = gasleft();
        (result[0], result[1], result[2], result[3], result[4]) =
            BabyBearEqPackedSquare._evaluateFixedEqTermsBlobRaw(
                statement, 0, ood[0], ood[1], ood[2], ood[3], point
            );
        used = start - gasleft();
    }
}

library BabyBearEqPackedEq {
    function _eqTerm(uint256 a, uint256 b) internal pure returns (uint256) {
        return BabyBearPackedField.eq(a, b);
    }

    function _evaluateFixedEqTermsBlobRaw(
        bytes calldata blob,
        uint256 statementPointOffset,
        uint256 initialOodPoint,
        uint256 round0OodPoint,
        uint256 round1OodPoint,
        uint256 round2OodPoint,
        uint256[] memory fullPoint
    )
        internal
        pure
        returns (
            uint256 statementEq,
            uint256 initialEq,
            uint256 round0Eq,
            uint256 round1Eq,
            uint256 round2Eq
        )
    {
        statementEq = BabyBearBenchField.ONE;
        initialEq = BabyBearBenchField.ONE;
        round0Eq = BabyBearBenchField.ONE;
        round1Eq = BabyBearBenchField.ONE;
        round2Eq = BabyBearBenchField.ONE;

        uint256 initialCurrent = initialOodPoint;
        uint256 round0Current = round0OodPoint;
        uint256 round1Current = round1OodPoint;
        uint256 round2Current = round2OodPoint;
        uint256 pointBase;
        assembly ("memory-safe") {
            pointBase := add(fullPoint, 0x20)
        }

        unchecked {
            {
                uint256 q;
                uint256 statementPointValue;
                assembly ("memory-safe") {
                    q := mload(add(pointBase, 672))
                    statementPointValue := and(
                        calldataload(add(add(blob.offset, statementPointOffset), 420)),
                        not(sub(shl(96, 1), 1))
                    )
                }
                statementEq = _eqTerm(statementPointValue, q);
                initialEq = _eqTerm(initialCurrent, q);
                round0Eq = _eqTerm(round0Current, q);
                round1Eq = _eqTerm(round1Current, q);
                round2Eq = _eqTerm(round2Current, q);
                initialCurrent = BabyBearPackedField.square(initialCurrent);
                round0Current = BabyBearPackedField.square(round0Current);
                round1Current = BabyBearPackedField.square(round1Current);
                round2Current = BabyBearPackedField.square(round2Current);
            }
            for (uint256 i = 21; i > 0; --i) {
                uint256 q;
                uint256 statementPointValue;
                assembly ("memory-safe") {
                    let idx := sub(i, 1)
                    q := mload(add(pointBase, shl(5, idx)))
                    statementPointValue := and(
                        calldataload(add(add(blob.offset, statementPointOffset), mul(20, idx))),
                        not(sub(shl(96, 1), 1))
                    )
                }

                statementEq = BabyBearPackedField.mul(statementEq, _eqTerm(statementPointValue, q));
                initialEq = BabyBearPackedField.mul(initialEq, _eqTerm(initialCurrent, q));
                initialCurrent = BabyBearPackedField.square(initialCurrent);

                if (i > 4) {
                    round0Eq = BabyBearPackedField.mul(round0Eq, _eqTerm(round0Current, q));
                    round0Current = BabyBearPackedField.square(round0Current);
                    if (i > 8) {
                        round1Eq = BabyBearPackedField.mul(round1Eq, _eqTerm(round1Current, q));
                        round1Current = BabyBearPackedField.square(round1Current);
                        if (i > 12) {
                            round2Eq = BabyBearPackedField.mul(round2Eq, _eqTerm(round2Current, q));
                            round2Current = BabyBearPackedField.square(round2Current);
                        }
                    }
                }
            }
        }
    }
}

contract BabyBearEqPackedEqHarness {
    function mul(uint256 a, uint256 b) external pure returns (uint256) {
        return BabyBearPackedField.mul(a, b);
    }

    function square(uint256 a) external pure returns (uint256) {
        return BabyBearPackedField.square(a);
    }

    function eq(uint256 a, uint256 b) external pure returns (uint256) {
        return BabyBearEqPackedEq._eqTerm(a, b);
    }

    function squares(uint256 a, uint256 n) external view returns (uint256 used, uint256 result) {
        uint256 start = gasleft();
        result = a;
        for (uint256 i; i < n; ++i) {
            result = BabyBearPackedField.square(result);
        }
        used = start - gasleft();
    }

    function prepare(bytes calldata statement, uint256[4] memory ood, uint256[] memory point)
        external
        view
        returns (uint256 used, uint256[5] memory result)
    {
        require(statement.length == 440 && point.length == 22, "SHAPE");
        uint256 start = gasleft();
        (result[0], result[1], result[2], result[3], result[4]) =
            BabyBearEqPackedEq._evaluateFixedEqTermsBlobRaw(
                statement, 0, ood[0], ood[1], ood[2], ood[3], point
            );
        used = start - gasleft();
    }
}

library BabyBearEqNoFinalSquares {
    function _eqTerm(uint256 a, uint256 b) internal pure returns (uint256) {
        return BabyBearPackedField.eq(a, b);
    }

    function _evaluateFixedEqTermsBlobRaw(
        bytes calldata blob,
        uint256 statementPointOffset,
        uint256 initialOodPoint,
        uint256 round0OodPoint,
        uint256 round1OodPoint,
        uint256 round2OodPoint,
        uint256[] memory fullPoint
    )
        internal
        pure
        returns (
            uint256 statementEq,
            uint256 initialEq,
            uint256 round0Eq,
            uint256 round1Eq,
            uint256 round2Eq
        )
    {
        statementEq = BabyBearBenchField.ONE;
        initialEq = BabyBearBenchField.ONE;
        round0Eq = BabyBearBenchField.ONE;
        round1Eq = BabyBearBenchField.ONE;
        round2Eq = BabyBearBenchField.ONE;

        uint256 initialCurrent = initialOodPoint;
        uint256 round0Current = round0OodPoint;
        uint256 round1Current = round1OodPoint;
        uint256 round2Current = round2OodPoint;
        uint256 pointBase;
        assembly ("memory-safe") {
            pointBase := add(fullPoint, 0x20)
        }

        unchecked {
            {
                uint256 q;
                uint256 statementPointValue;
                assembly ("memory-safe") {
                    q := mload(add(pointBase, 672))
                    statementPointValue := and(
                        calldataload(add(add(blob.offset, statementPointOffset), 420)),
                        not(sub(shl(96, 1), 1))
                    )
                }
                statementEq = _eqTerm(statementPointValue, q);
                initialEq = _eqTerm(initialCurrent, q);
                round0Eq = _eqTerm(round0Current, q);
                round1Eq = _eqTerm(round1Current, q);
                round2Eq = _eqTerm(round2Current, q);
                initialCurrent = BabyBearPackedField.square(initialCurrent);
                round0Current = BabyBearPackedField.square(round0Current);
                round1Current = BabyBearPackedField.square(round1Current);
                round2Current = BabyBearPackedField.square(round2Current);
            }
            for (uint256 i = 21; i > 0; --i) {
                uint256 q;
                uint256 statementPointValue;
                assembly ("memory-safe") {
                    let idx := sub(i, 1)
                    q := mload(add(pointBase, shl(5, idx)))
                    statementPointValue := and(
                        calldataload(add(add(blob.offset, statementPointOffset), mul(20, idx))),
                        not(sub(shl(96, 1), 1))
                    )
                }

                statementEq = BabyBearPackedField.mul(statementEq, _eqTerm(statementPointValue, q));
                initialEq = BabyBearPackedField.mul(initialEq, _eqTerm(initialCurrent, q));
                if (i > 1) initialCurrent = BabyBearPackedField.square(initialCurrent);

                if (i > 4) {
                    round0Eq = BabyBearPackedField.mul(round0Eq, _eqTerm(round0Current, q));
                    if (i > 5) round0Current = BabyBearPackedField.square(round0Current);
                    if (i > 8) {
                        round1Eq = BabyBearPackedField.mul(round1Eq, _eqTerm(round1Current, q));
                        if (i > 9) round1Current = BabyBearPackedField.square(round1Current);
                        if (i > 12) {
                            round2Eq = BabyBearPackedField.mul(round2Eq, _eqTerm(round2Current, q));
                            if (i > 13) round2Current = BabyBearPackedField.square(round2Current);
                        }
                    }
                }
            }
        }
    }
}

contract BabyBearEqNoFinalSquaresHarness {
    function mul(uint256 a, uint256 b) external pure returns (uint256) {
        return BabyBearPackedField.mul(a, b);
    }

    function square(uint256 a) external pure returns (uint256) {
        return BabyBearPackedField.square(a);
    }

    function eq(uint256 a, uint256 b) external pure returns (uint256) {
        return BabyBearEqNoFinalSquares._eqTerm(a, b);
    }

    function squares(uint256 a, uint256 n) external view returns (uint256 used, uint256 result) {
        uint256 start = gasleft();
        result = a;
        for (uint256 i; i < n; ++i) {
            result = BabyBearPackedField.square(result);
        }
        used = start - gasleft();
    }

    function prepare(bytes calldata statement, uint256[4] memory ood, uint256[] memory point)
        external
        view
        returns (uint256 used, uint256[5] memory result)
    {
        require(statement.length == 440 && point.length == 22, "SHAPE");
        uint256 start = gasleft();
        (result[0], result[1], result[2], result[3], result[4]) =
            BabyBearEqNoFinalSquares._evaluateFixedEqTermsBlobRaw(
                statement, 0, ood[0], ood[1], ood[2], ood[3], point
            );
        used = start - gasleft();
    }
}

library BabyBearEqPackedMulNoFinal {
    function _eqTerm(uint256 p, uint256 q) internal pure returns (uint256) {
        uint256 out;
        assembly ("memory-safe") {
            let M := 0x78000001
            let mask := 0xffffffff

            let p0 := shr(224, p)
            let p1 := and(shr(192, p), mask)
            let p2 := and(shr(160, p), mask)
            let p3 := and(shr(128, p), mask)
            let p4 := and(shr(96, p), mask)

            let q0 := shr(224, q)
            let q1 := and(shr(192, q), mask)
            let q2 := and(shr(160, q), mask)
            let q3 := and(shr(128, q), mask)
            let q4 := and(shr(96, q), mask)

            let c0 := mul(p0, q0)
            let c1 := add(mul(p0, q1), mul(p1, q0))
            let c2 := add(add(mul(p0, q2), mul(p1, q1)), mul(p2, q0))
            let c3 := add(add(add(mul(p0, q3), mul(p1, q2)), mul(p2, q1)), mul(p3, q0))
            let c4 :=
                add(add(add(add(mul(p0, q4), mul(p1, q3)), mul(p2, q2)), mul(p3, q1)), mul(p4, q0))
            let c5 := add(add(add(mul(p1, q4), mul(p2, q3)), mul(p3, q2)), mul(p4, q1))
            let c6 := add(add(mul(p2, q4), mul(p3, q3)), mul(p4, q2))
            let c7 := add(mul(p3, q4), mul(p4, q3))
            let c8 := mul(p4, q4)
            let m0 := add(c0, shl(1, c5))
            let m1 := add(c1, shl(1, c6))
            let m2 := add(c2, shl(1, c7))
            let m3 := add(c3, shl(1, c8))
            let m4 := c4
            let b := shl(2, M)
            let o0 := mod(add(add(add(1, shl(1, m0)), b), sub(0, add(p0, q0))), M)
            let o1 := mod(add(add(shl(1, m1), b), sub(0, add(p1, q1))), M)
            let o2 := mod(add(add(shl(1, m2), b), sub(0, add(p2, q2))), M)
            let o3 := mod(add(add(shl(1, m3), b), sub(0, add(p3, q3))), M)
            let o4 := mod(add(add(shl(1, m4), b), sub(0, add(p4, q4))), M)

            out := or(
                or(or(shl(224, o0), shl(192, o1)), or(shl(160, o2), shl(128, o3))),
                shl(96, o4)
            )
        }
        return out;
    }

    function _evaluateFixedEqTermsBlobRaw(
        bytes calldata blob,
        uint256 statementPointOffset,
        uint256 initialOodPoint,
        uint256 round0OodPoint,
        uint256 round1OodPoint,
        uint256 round2OodPoint,
        uint256[] memory fullPoint
    )
        internal
        pure
        returns (
            uint256 statementEq,
            uint256 initialEq,
            uint256 round0Eq,
            uint256 round1Eq,
            uint256 round2Eq
        )
    {
        statementEq = BabyBearBenchField.ONE;
        initialEq = BabyBearBenchField.ONE;
        round0Eq = BabyBearBenchField.ONE;
        round1Eq = BabyBearBenchField.ONE;
        round2Eq = BabyBearBenchField.ONE;

        uint256 initialCurrent = initialOodPoint;
        uint256 round0Current = round0OodPoint;
        uint256 round1Current = round1OodPoint;
        uint256 round2Current = round2OodPoint;
        uint256 pointBase;
        assembly ("memory-safe") {
            pointBase := add(fullPoint, 0x20)
        }

        unchecked {
            {
                uint256 q;
                uint256 statementPointValue;
                assembly ("memory-safe") {
                    q := mload(add(pointBase, 672))
                    statementPointValue := and(
                        calldataload(add(add(blob.offset, statementPointOffset), 420)),
                        not(sub(shl(96, 1), 1))
                    )
                }
                statementEq = _eqTerm(statementPointValue, q);
                initialEq = _eqTerm(initialCurrent, q);
                round0Eq = _eqTerm(round0Current, q);
                round1Eq = _eqTerm(round1Current, q);
                round2Eq = _eqTerm(round2Current, q);
                initialCurrent = BabyBearBenchField.square(initialCurrent);
                round0Current = BabyBearBenchField.square(round0Current);
                round1Current = BabyBearBenchField.square(round1Current);
                round2Current = BabyBearBenchField.square(round2Current);
            }
            for (uint256 i = 21; i > 0; --i) {
                uint256 q;
                uint256 statementPointValue;
                assembly ("memory-safe") {
                    let idx := sub(i, 1)
                    q := mload(add(pointBase, shl(5, idx)))
                    statementPointValue := and(
                        calldataload(add(add(blob.offset, statementPointOffset), mul(20, idx))),
                        not(sub(shl(96, 1), 1))
                    )
                }

                statementEq = BabyBearPackedField.mul(statementEq, _eqTerm(statementPointValue, q));
                initialEq = BabyBearPackedField.mul(initialEq, _eqTerm(initialCurrent, q));
                if (i > 1) initialCurrent = BabyBearBenchField.square(initialCurrent);

                if (i > 4) {
                    round0Eq = BabyBearPackedField.mul(round0Eq, _eqTerm(round0Current, q));
                    if (i > 5) round0Current = BabyBearBenchField.square(round0Current);
                    if (i > 8) {
                        round1Eq = BabyBearPackedField.mul(round1Eq, _eqTerm(round1Current, q));
                        if (i > 9) round1Current = BabyBearBenchField.square(round1Current);
                        if (i > 12) {
                            round2Eq = BabyBearPackedField.mul(round2Eq, _eqTerm(round2Current, q));
                            if (i > 13) round2Current = BabyBearBenchField.square(round2Current);
                        }
                    }
                }
            }
        }
    }
}

contract BabyBearEqPackedMulNoFinalHarness {
    function mul(uint256 a, uint256 b) external pure returns (uint256) {
        return BabyBearPackedField.mul(a, b);
    }

    function square(uint256 a) external pure returns (uint256) {
        return BabyBearBenchField.square(a);
    }

    function eq(uint256 a, uint256 b) external pure returns (uint256) {
        return BabyBearEqPackedMulNoFinal._eqTerm(a, b);
    }

    function squares(uint256 a, uint256 n) external view returns (uint256 used, uint256 result) {
        uint256 start = gasleft();
        result = a;
        for (uint256 i; i < n; ++i) {
            result = BabyBearBenchField.square(result);
        }
        used = start - gasleft();
    }

    function prepare(bytes calldata statement, uint256[4] memory ood, uint256[] memory point)
        external
        view
        returns (uint256 used, uint256[5] memory result)
    {
        require(statement.length == 440 && point.length == 22, "SHAPE");
        uint256 start = gasleft();
        (result[0], result[1], result[2], result[3], result[4]) =
            BabyBearEqPackedMulNoFinal._evaluateFixedEqTermsBlobRaw(
                statement, 0, ood[0], ood[1], ood[2], ood[3], point
            );
        used = start - gasleft();
    }
}
